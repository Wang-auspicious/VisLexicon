# radix-primitives review · 2026-09-06 · swarm-reviewer-grok-c

APPROVED。`curatorId` `swarm-batch-radix-split` ≠ `reviewerId`。

活页重开：产品页 title `Radix Primitives`；Introduction 写明 low-level / Accessible / Unstyled；Dialog 页有 `import { Dialog } from "radix-ui"` 与焦点陷阱。截图 sha256 匹配。

facts：WorkOS / MIT / 包名 `radix-ui` 与 LICENSE Copyright (c) 2022 WorkOS 核到。

复核改动：`entityId` → `entity-radix-ui`。breadth `alt` 改为首屏可见的 Introduction / Vision / Accessible 段（Unstyled 在首屏以下）。confirmed，清空 alternatives。

入组 `components-and-blocks`。
