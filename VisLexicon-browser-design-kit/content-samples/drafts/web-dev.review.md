# web-dev review · 2026-09-06 · swarm-reviewer-grok-c

APPROVED。`curatorId` `swarm-batch-radix-split` ≠ `reviewerId`。

活页重开：首页 `Building a better web, together`；Learn 课程卡；Welcome to Learn Accessibility 定义 a11y 并链 WCAG。截图为英文首屏，sha256 匹配。

facts：Chrome DevRel / Google；旧仓库 `GoogleChrome/web.dev` 已归档；LICENSE 正文 CC BY 3.0 + 代码示例 Apache-2.0。分类树无 CC-BY-3.0，`facts.license` 记 `custom` 成立。

复核改动：breadth `<title>` 改为 `Learn web development | web.dev`；author evidence 按当前 About 页署名改写。主类保持 `learning-editorial` / `tutorials-courses-workshops`。入组 `references-and-standards`。
