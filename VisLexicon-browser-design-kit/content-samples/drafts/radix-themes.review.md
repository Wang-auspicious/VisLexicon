# radix-themes review · 2026-09-06 · swarm-reviewer-grok-c

APPROVED。`curatorId` `swarm-batch-radix-split` ≠ `reviewerId`。

活页重开：首页 title `Radix UI`；Getting started / Dialog 文档与 JSON 一致。截图 1280×900，sha256 与 bytes 重算匹配。三 URL 三画面。

facts：WorkOS / MIT / `@radix-ui/themes` / 首页 Pricing 卡为演示，均可在 sourceUrl 核到。

复核改动：`entityId` 改为 `entity-radix-ui`（四条产品共用实体，entryId 保持 `radix-themes`）。`classification.status` confirmed，清空 alternatives。

入组 `components-and-blocks`。未发布混合 `radix-ui`。
