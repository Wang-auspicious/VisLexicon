# radix-icons review · 2026-09-06 · swarm-reviewer-grok-c

APPROVED。`curatorId` `swarm-batch-radix-split` ≠ `reviewerId`。

活页重开：Icons 页 title `Radix Icons`，可见 15×15 网格与 `npm i @radix-ui/react-icons`。仓库 title `GitHub - radix-ui/icons: A crisp set of 15×15 icons designed by the @workos team.`。packages 目录含 `generate-icon-lib` 与 `radix-icons`。三张 PNG sha256 匹配。

官网是单页画廊，breadth/proof 用 GitHub 补范围与发布包，三 URL 不重复。lucide 已发布、remixicon 仍在 drafts，图标组不足 3 条，本条不入组。

复核改动：`entityId` → `entity-radix-ui`。confirmed。
