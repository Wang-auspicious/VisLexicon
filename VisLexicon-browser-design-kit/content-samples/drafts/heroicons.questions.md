# heroicons blockers · swarm-reviewer-grok-b · 2026-09-05

不能 APPROVED。三张截图不是三页证据。

1. `frontend/public/shots/heroicons/v2-identity.png`、`v2-breadth.png`、`v2-proof.png` 字节与 sha256 完全相同：`fcc334704c564b69cb536dc7745fb0eec0a61ebe3a6c4be1005e319dee3d44c6`（176180 bytes，1280×900）。画面都是首页 Outline 网格。
2. `capture-jobs.json` 把三页都写成 `https://heroicons.com/`，没有打开草稿声称的 `/solid` 与 `/home`。
3. 需要重截三张互异首屏后再复核：首页 Outline、Solid 目录、单图标路径。`/home` 是否真打开单个图标，必须用截图证明，不能只凭 SPA 路由 200。

保持 DRAFT。勿写入 `approved-v3`。
