# D3 进站报告 · 2026-09-05

访问方式：真实 HTTP GET。未改 `approved-v3`、`collections.js`、jsx、css。`status` 保持 `DRAFT`，无 `reviewerId`。

## 打开过的页

| 角色 | URL | 最终地址 | `<title>` | HTTP |
|---|---|---|---|---|
| 首页 / identity | https://d3js.org/ | https://d3js.org/ | D3 by Observable \| The JavaScript library for bespoke data visualization | 200 |
| API index / breadth | https://d3js.org/api | 同左 | API index \| D3 by Observable | 200 |
| d3-scale / proof | https://d3js.org/d3-scale | 同左 | d3-scale \| D3 by Observable | 200 |
| 仓库 | https://github.com/d3/d3 | 同左 | GitHub - d3/d3: Bring data to life with SVG, Canvas and HTML. | 200 |
| LICENSE | https://raw.githubusercontent.com/d3/d3/main/LICENSE | 同左 | （纯文本 ISC） | 200 |
| Observable Gallery | https://observablehq.com/@d3/gallery | — | 429 Too Many Requests，改用站内 API index | 429 |

无登录墙。文档公开。

## 首页看到什么

`<title>` = D3 by Observable | The JavaScript library for bespoke data visualization。大标题 **D3**，副标题 **The JavaScript library for bespoke data visualization**。入口：Get started `/getting-started`、What is D3? `/what-is-d3`、Examples 链到 Observable gallery。模块卡：Selections and transitions → `/d3-selection`；Scales and axes → `/d3-scale`；Shapes → `/d3-shape`；Layouts → `/d3-hierarchy`；Geographic maps → `/d3-geo`。页内写 **Built by Observable**、**Powering Observable Plot**。

## API 与模块

API index（224145 B）列出 `d3-axis`、`d3-chord`、`d3-scale` 等模块。d3-scale 页含 Introduction、linear、quantize；正文：**Scales map a dimension of abstract data to a visual representation.**

## 许可与价格

LICENSE：**Copyright 2010-2023 Mike Bostock**，ISC 许可措辞（Permission to use, copy, modify, and/or distribute… with or without fee）。package.json：`name` d3，`version` 7.9.0，`license` ISC，`author.name` Mike Bostock，`repository.url` https://github.com/d3/d3.git。文档免费公开。

## 截图

三张均 1280×900 PNG。identity 为 bespoke data visualization 与六边形示例。breadth 为 API index。proof 为 d3-scale。

## 分类与人话

- 一级 `visual-implementation` / 小类 `data-visualization-code`。分类 `needs-review`。
- `atlasTerms` 空。

## 未做

Gallery 429，breadth 改用站内 API index。未写入 `approved-v3`。
