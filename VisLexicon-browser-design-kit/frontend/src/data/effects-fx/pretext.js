// pretext 风格排版引擎族：把"字怎么摆"当成一等公民（Knuth-Plass 断行、度量、动画化排版）
const P = (k, zh, en, min, max, step, def, unit) => ({ k, zh, en, min, max, step, def, unit: unit || '' });
const E = 'cubic-bezier(.22,1,.36,1)';
const EXPO = 'cubic-bezier(.16,1,.3,1)';
const I = (id, zh, en, dz, de, pz, pe, demo, css, params) => ({ id, zh, en, dz, de, pz, pe, demo, css, params });

export default {
  id: 'pretext', zh: '排版引擎', en: 'Typesetting engine',
  dz: 'pretext 那一派：断行、度量、基线都可计算，也都可以动起来', de: 'The pretext school — line breaking, metrics and baselines as computable, animatable state',
  items: [
    I('knuthplass', '全段最优断行', 'Knuth–Plass breaking', '整段一起算，不是一行一行贪心', 'The paragraph is optimised as a whole',
      '给每个断点算 badness，动态规划求全段最小总代价。', 'Score each breakpoint by badness and dynamic-program the paragraph.',
      'text', `@keyframes fxk{0%,100%{letter-spacing:-.03em}50%{letter-spacing:.005em}}.fx{font-size:clamp(30px,5vw,58px);white-space:normal;max-width:9ch;line-height:1.06;animation:fxk 5s ease-in-out infinite}`,
      [P('w', '栏宽', 'Measure', 6, 16, 1, 9, 'ch')]),
    I('balancetitle', '标题均衡', 'Balanced headline', '两行长短接近才像标题', 'Lines of similar length read as a title',
      'text-wrap:balance，或自己二分栏宽求最小行长方差。', 'text-wrap: balance, or binary-search the measure for minimal variance.',
      'text', `@keyframes fxk{0%,100%{max-width:14ch}50%{max-width:9ch}}.fx{font-size:clamp(26px,4.4vw,46px);white-space:normal;text-wrap:balance;line-height:1.1;animation:fxk 5s ${E} infinite}`),
    I('prettywrap', '避免孤字', 'No orphan word', '最后一行不能只剩一个词', 'The last line never holds one word',
      'text-wrap:pretty；引擎侧则给末行加惩罚项。', 'text-wrap: pretty — engine-side, penalise short last lines.',
      'text', `@keyframes fxk{0%,100%{max-width:16ch}50%{max-width:13ch}}.fx{font-size:clamp(22px,3.4vw,34px);font-weight:500;white-space:normal;text-wrap:pretty;line-height:1.35;animation:fxk 6s ${E} infinite}`),
    I('hyphenate', '连字断词', 'Hyphenation', '窄栏靠断词救回来', 'Narrow columns need hyphens',
      'Liang 算法查词典给断点，最少留 2 / 3 个字母。', 'Liang patterns for breakpoints; keep 2 before, 3 after.',
      'text', `@keyframes fxk{0%,100%{max-width:8ch}50%{max-width:6ch}}.fx{font-size:clamp(24px,4vw,40px);white-space:normal;hyphens:auto;line-height:1.15;animation:fxk 5s ${E} infinite}`),
    I('justifyrivers', '两端对齐去白河', 'Rivers-free justification', '别让空白连成一条河', 'No white river down the column',
      '词距惩罚 + 相邻行空隙对齐检测，必要时改断点。', 'Penalise word-space stretch and detect vertically aligned gaps.',
      'text', `@keyframes fxk{0%,100%{word-spacing:0}50%{word-spacing:.22em}}.fx{font-size:clamp(20px,3vw,30px);font-weight:400;white-space:normal;text-align:justify;max-width:15ch;line-height:1.4;animation:fxk 4.6s ease-in-out infinite}`),
    I('measuretext', '离屏度量', 'Offscreen measurement', '排版前先量，量完一次成型', 'Measure first, lay out once',
      'canvas measureText 或一次性 Range.getClientRects，别循环读 layout。', 'Use canvas measureText or one batched Range.getClientRects.',
      'field', `@keyframes fxk{0%,100%{width:36%}50%{width:82%}}.fx>u{width:2px}.fx::after{content:'';position:absolute;height:2px;background:#e5a68f;bottom:10px;left:14px;animation:fxk 3s ${E} infinite}.fx{position:relative}`),
    I('shrinkwrap', '包裹到字宽', 'Shrink-wrap box', '框贴着字，不多一像素', 'The box hugs the text exactly',
      'width:fit-content 或按最长行宽度定框，气泡才不虚胖。', 'fit-content, or size the box by the longest line.',
      'pill', `@keyframes fxk{0%,100%{max-width:180px}50%{max-width:96px}}.fxpill{max-width:220px;animation:fxk 5s ${E} infinite}.fx{white-space:nowrap}`),
    I('fittobox', '装满容器', 'Fit to box', '字号自动缩到刚好装满', 'Type scales to fill exactly',
      '二分字号直到测量宽高刚好不溢出，缓存结果。', 'Binary-search the size until it just fits; cache the result.',
      'text', `@keyframes fxk{0%,100%{font-size:clamp(30px,6vw,64px)}50%{font-size:clamp(20px,3.6vw,38px)}}.fx{animation:fxk 5s ${E} infinite}`),
    I('baselinegrid', '基线网格', 'Baseline grid', '所有文字都落在同一套横线上', 'Every line lands on the same rails',
      '行高取网格整数倍（如 4px 的 6 倍），首行做 half-leading 补偿。', 'Line-height as a multiple of the grid; compensate the first line.',
      'list', `@keyframes fxk{0%,100%{background-position:0 0}50%{background-position:0 4px}}.fxcol{background:repeating-linear-gradient(180deg,rgba(58,95,122,.1) 0 1px,transparent 1px 24px);animation:fxk 4s ease-in-out infinite}.fx{background:transparent;border-color:transparent}`),
    I('halfleading', '半行距补偿', 'Half-leading trim', '首行上方那点多余空白要去掉', 'Kill the extra space above the first line',
      'text-box-trim / leading-trim，或用 cap-height 手算偏移。', 'text-box-trim, or offset by cap-height manually.',
      'text', `@keyframes fxk{0%,100%{transform:translateY(0)}50%{transform:translateY(-.06em)}}.fx{animation:fxk 4s ease-in-out infinite}`),
    I('opticalmargin', '视觉边界对齐', 'Optical margin', '引号和 A 要挂到栏外一点', 'Quotes and A hang past the margin',
      '按字形左右侧承（side bearing）负外移，别整体缩进。', 'Nudge by side bearing, not by indenting the block.',
      'text', `@keyframes fxk{0%,100%{transform:translateX(0)}50%{transform:translateX(-.06em)}}.fx>span:first-child{display:inline-block;animation:fxk 3.4s ease-in-out infinite}`),
    I('hangpunct', '标点悬挂', 'Hanging punctuation', '行首标点挂出去', 'Leading punctuation hangs outside',
      'hanging-punctuation:first last，中文引号尤其需要。', 'hanging-punctuation: first last — essential for CJK quotes.',
      'text', `@keyframes fxk{0%,100%{text-indent:0}50%{text-indent:-.36em}}.fx{font-size:clamp(24px,4vw,40px);white-space:normal;max-width:12ch;hanging-punctuation:first last;animation:fxk 4.6s ${E} infinite}`),
    I('cjkcompress', '标点挤压', 'CJK punctuation squeeze', '中文全角标点该压掉一半', 'Full-width punctuation gives back half its width',
      'text-spacing-trim 或按字形类别压缩标点前后间距。', 'text-spacing-trim, or compress by punctuation class.',
      'text', `@keyframes fxk{0%,100%{letter-spacing:0}50%{letter-spacing:-.08em}}.fx{font-size:clamp(26px,4.4vw,44px);animation:fxk 4s ease-in-out infinite}`),
    I('cjkspacing', '中西文间隙', 'CJK–Latin spacing', '汉字与英文之间要留四分之一空', 'A quarter space between Han and Latin',
      'text-autospace / 手动插 U+2009，别用普通空格。', 'text-autospace, or insert a thin space — not a normal one.',
      'text', `@keyframes fxk{0%,100%{word-spacing:0}50%{word-spacing:.12em}}.fx{font-size:clamp(24px,4vw,40px);animation:fxk 4s ease-in-out infinite}`),
    I('kerningpair', '字距对', 'Kerning pairs', 'AV、To 这类组合要收紧', 'AV and To need pulling together',
      'font-kerning:normal 交给字体的 kern 表，别全局加 letter-spacing。', 'font-kerning: normal — trust the font, not a global tracking value.',
      'text', `@keyframes fxk{0%,100%{letter-spacing:.02em}50%{letter-spacing:-.045em}}.fx{font-kerning:normal;animation:fxk 4.4s ease-in-out infinite}`),
    I('trackingscale', '字距随字号', 'Optical tracking', '字越大字距越紧', 'Bigger type, tighter tracking',
      'letter-spacing 用 em 并随 clamp 递减：标题 -0.035em，正文 0。', 'Track in em: −0.035em at display sizes, 0 in body.',
      'text', `@keyframes fxk{0%{font-size:clamp(18px,3vw,26px);letter-spacing:0}100%{font-size:clamp(38px,7vw,78px);letter-spacing:-.04em}}.fx{animation:fxk 3.4s ${E} infinite alternate}`),
    I('varfontfit', '可变字体挤压', 'Variable width fit', '用宽度轴而不是缩放', 'Use the width axis, not scale',
      'font-variation-settings 的 wdth 轴插值，字形不会变形。', 'Interpolate the wdth axis so glyphs never distort.',
      'text', `@keyframes fxk{0%,100%{font-stretch:100%;transform:scaleX(1)}50%{font-stretch:75%;transform:scaleX(.94)}}.fx{animation:fxk 4.4s ${E} infinite}`),
    I('varweight', '字重轴动画', 'Weight axis', '字重连续变化，不跳档', 'Weight moves continuously',
      'wght 轴可插值：400→700 之间任意值都合法。', 'The wght axis is continuous — 400 → 700 with no steps.',
      'text', `@keyframes fxk{0%,100%{font-weight:400}50%{font-weight:800}}.fx{animation:fxk 3.4s ease-in-out infinite}`),
    I('opticalsize', '光学尺寸轴', 'Optical size axis', '小字加粗一点才看得清', 'Small sizes need more weight',
      'opsz 轴让字体自己在小号时加粗、大号时收细。', 'The opsz axis thickens small text and refines display sizes.',
      'text', `@keyframes fxk{0%{font-size:clamp(16px,2.6vw,22px);font-weight:600}100%{font-size:clamp(40px,7.4vw,82px);font-weight:500}}.fx{animation:fxk 3.6s ${E} infinite alternate}`),
    I('tabularfig', '等宽数字', 'Tabular figures', '数字跳动时列不抖', 'Columns stop twitching',
      'font-variant-numeric:tabular-nums，所有会变的数字都要开。', 'tabular-nums on anything that changes.',
      'num', `@keyframes fxk{0%,100%{opacity:1}50%{opacity:.55}}.fx{font-variant-numeric:tabular-nums;animation:fxk 1.4s steps(2,end) infinite}`),
    I('smallcaps', '小型大写', 'Small caps', '真小型大写不是缩小的大写', 'Real small caps are not shrunk capitals',
      'font-variant-caps:small-caps，字体没这套就别硬做。', 'font-variant-caps: small-caps — skip it if the font lacks them.',
      'text', `@keyframes fxk{0%,100%{letter-spacing:.06em}50%{letter-spacing:.14em}}.fx{font-variant-caps:small-caps;font-size:clamp(26px,4.4vw,46px);animation:fxk 4.4s ease-in-out infinite}`),
    I('ligature', '连字开合', 'Ligature toggle', 'fi、ffl 要连起来', 'fi and ffl should join',
      'font-variant-ligatures:common-ligatures；等宽代码字体反而要关。', 'Enable common ligatures; disable them in code faces.',
      'text', `@keyframes fxk{0%,49%{font-variant-ligatures:none}50%,100%{font-variant-ligatures:common-ligatures}}.fx{animation:fxk 3s steps(1,end) infinite}`),
    I('dropcap', '首字下沉', 'Drop cap', '首字母占三行高', 'The first letter owns three lines',
      'initial-letter:3，配 initial-letter-align 对齐基线。', 'initial-letter: 3 with initial-letter-align.',
      'text', `@keyframes fxk{0%,100%{transform:scale(1)}50%{transform:scale(1.1)}}.fx{font-size:clamp(20px,3.2vw,30px);white-space:normal;max-width:14ch;line-height:1.4;text-align:left}.fx>span:first-child{float:left;font-size:2.6em;line-height:.86;padding-right:.06em;animation:fxk 4s ease-in-out infinite}`),
    I('firstlineindent', '首行缩进', 'First-line indent', '要么缩进，要么段间距，别都要', 'Indent or space — never both',
      'text-indent:1.5em 且段间距归零，是书籍排法。', 'text-indent 1.5em with zero paragraph margin.',
      'list', `@keyframes fxk{0%,100%{text-indent:0}50%{text-indent:1.6em}}.fxcol{gap:0}.fx{height:auto;padding:8px 14px;background:transparent;border:0;font-size:13px;animation:fxk 4s ${E} infinite}`),
    I('hangindent', '悬挂缩进', 'Hanging indent', '项目符号挂在文本外', 'Bullets hang outside the text block',
      'padding-left + text-indent 负值，或 list-style-position:outside。', 'Positive padding with a negative text-indent.',
      'list', `@keyframes fxk{0%,100%{padding-left:14px}50%{padding-left:34px}}.fx{animation:fxk 4s ${E} infinite;animation-delay:calc(var(--i)*-120ms)}`),
    I('columnbalance', '多栏均衡', 'Column balance', '最后一栏不能空一半', 'The last column must not be half empty',
      'column-fill:balance，或计算后强制断栏。', 'column-fill: balance, or force breaks after measuring.',
      'grid', `@keyframes fxk{0%,100%{transform:scaleY(1)}50%{transform:scaleY(.72)}}.fxgrid{grid-template-columns:repeat(3,92px);align-items:start}.fx{height:120px;transform-origin:50% 0;animation:fxk 4.4s ${E} infinite;animation-delay:calc(var(--i)*-100ms)}`),
    I('widowcontrol', '寡行控制', 'Widow control', '别让一行孤零零跑到下一栏', 'No single line stranded in the next column',
      'orphans / widows:2，分栏与分页都生效。', 'orphans and widows: 2 — works for columns and print.',
      'scroll', `@keyframes fxk{0%,100%{transform:translateY(0)}50%{transform:translateY(-12px)}}.fx{height:96px;animation:fxk 4s ${E} infinite;animation-delay:calc(var(--i)*-90ms)}`),
    I('footnoteflow', '脚注回流', 'Footnote flow', '脚注跟着引用所在页', 'Notes stay with their reference',
      '先排正文测高，再把脚注塞进剩余高度，超出则往后推。', 'Lay out body first, then fit notes into the leftover height.',
      'scroll', `@keyframes fxk{0%,100%{height:120px}50%{height:88px}}.fx:first-child{animation:fxk 4.4s ${E} infinite}`),
    I('runningheader', '书眉', 'Running header', '页眉写着当前章节', 'The header knows its section',
      '排版时记录每页首个标题，回填到页眉。', 'Record the first heading per page and back-fill the header.',
      'nav', `@keyframes fxk{0%,100%{opacity:1}48%{opacity:0}52%{opacity:0}}.fx:first-child{animation:fxk 4s steps(1,end) infinite}`),
    I('textonpath', '沿路径排字', 'Text on a path', '字跟着曲线走', 'Letters ride the curve',
      'SVG textPath + startOffset；每字自转要单独算切线。', 'SVG textPath with startOffset; per-glyph rotation needs tangents.',
      'text', `@keyframes fxk{0%,100%{transform:rotate(-6deg)}50%{transform:rotate(6deg)}}.fx>span{display:inline-block;transform-origin:50% 200%;transform:rotate(calc((var(--i) - 3)*6deg));animation:fxk 5s ease-in-out infinite}`),
    I('circletype', '环形排字', 'Circular type', '一圈字围成徽章', 'Type wrapped into a seal',
      '每字 rotate(i*360/n) + translateY(-r)，字距按弧长算。', 'rotate(i · 360/n) with translateY(−r); space by arc length.',
      'text', `@keyframes fxk{from{transform:rotate(0)}to{transform:rotate(360deg)}}.fx{font-size:clamp(20px,3vw,28px);animation:fxk 18s linear infinite}.fx>span{display:inline-block;position:absolute;left:50%;top:50%;transform-origin:0 0;transform:rotate(calc(var(--i)*40deg)) translate(0,-90px)}.fxtext{position:relative;width:220px;height:220px}`),
    I('vertical', '竖排', 'Vertical writing', '中文竖着读', 'CJK read top to bottom',
      'writing-mode:vertical-rl，数字用 text-combine-upright。', 'writing-mode: vertical-rl; combine digits upright.',
      'text', `@keyframes fxk{0%,100%{letter-spacing:.02em}50%{letter-spacing:.16em}}.fx{writing-mode:vertical-rl;font-size:clamp(22px,3.6vw,34px);white-space:nowrap;animation:fxk 4.4s ease-in-out infinite}`),
    I('rtlmix', '双向文本', 'Bidi text', '阿拉伯语和数字混排不乱', 'Arabic and digits keep their order',
      'dir="auto" + Unicode 隔离符 U+2068/2069，别手动反转。', 'dir="auto" with isolate marks — never reverse strings by hand.',
      'text', `@keyframes fxk{0%,100%{transform:translateX(0)}50%{transform:translateX(6px)}}.fx{direction:rtl;unicode-bidi:isolate;animation:fxk 4s ease-in-out infinite}`),
    I('lineheightfluid', '流体行高', 'Fluid leading', '窄屏行高大一点', 'Narrow screens need more leading',
      'line-height:calc(1.2 + .35*(60ch - 100%)/40ch) 之类的插值。', 'Interpolate leading against the measure, not the viewport.',
      'text', `@keyframes fxk{0%,100%{line-height:1.12;max-width:12ch}50%{line-height:1.42;max-width:8ch}}.fx{font-size:clamp(22px,3.6vw,34px);white-space:normal;animation:fxk 5s ${E} infinite}`),
    I('measurelimit', '栏宽上限', 'Measure cap', '一行 45–75 个字符最好读', '45–75 characters per line',
      'max-width:65ch 是最省事的可读性修复。', 'max-width: 65ch is the cheapest readability fix there is.',
      'text', `@keyframes fxk{0%,100%{max-width:22ch}50%{max-width:12ch}}.fx{font-size:clamp(18px,2.6vw,24px);font-weight:400;white-space:normal;line-height:1.5;animation:fxk 6s ${E} infinite}`),
    I('layoutspring', '排版弹簧过渡', 'Springy relayout', '换栏宽时每行弹到新位置', 'Lines spring to their new places',
      '断行结果变化时，对每行的 y 用弹簧插值而不是重绘。', 'Spring each line to its new y instead of repainting.',
      'list', `@keyframes fxk{0%{transform:translateY(12px);opacity:.4}60%{transform:translateY(-3px);opacity:1}100%{transform:translateY(0)}}.fx{animation:fxk 1s cubic-bezier(.2,1.3,.3,1) infinite alternate;animation-delay:calc(var(--i)*50ms)}`),
    I('glyphinterp', '字形插值', 'Glyph interpolation', '两个字之间连续变形', 'One glyph melts into another',
      '同一可变字体的两个实例插值，跨字体要先做轮廓匹配。', 'Interpolate two instances of one variable font.',
      'text', `@keyframes fxk{0%,100%{font-weight:300;font-stretch:112%}50%{font-weight:800;font-stretch:82%}}.fx{animation:fxk 4.4s ease-in-out infinite}`),
    I('incrementallayout', '增量重排', 'Incremental relayout', '只重排改动的那几段', 'Only the touched paragraphs re-lay out',
      '缓存每段的断行结果，脏标记驱动局部重排。', 'Cache per-paragraph breaks and re-run only the dirty ones.',
      'list', `@keyframes fxk{0%,100%{background:#fcfcfb}50%{background:#f4f6ee}}.fx:nth-child(3),.fx:nth-child(4){animation:fxk 2.6s ease-in-out infinite}`),
    I('virtualtext', '长文虚拟化', 'Virtualised text', '十万行也能秒滚', 'Instant scroll on 100k lines',
      '按行高预估总高，只渲染可视窗口 ± 缓冲。', 'Estimate total height by line height; render the window plus buffer.',
      'scroll', `@keyframes fxk{from{opacity:0}to{opacity:1}}.fx{height:64px;animation:fxk .4s linear both;animation-timeline:view();animation-range:entry 0% entry 40%}`),
    I('nolayoutthrash', '避免布局抖动', 'No layout thrash', '读写分离，一帧只布局一次', 'Read then write, one layout per frame',
      '先批量读 rect，再批量改样式，别交替。', 'Batch all reads, then all writes — never interleave.',
      'grid', `@keyframes fxk{0%,100%{outline:1px solid rgba(142,166,4,0)}50%{outline:1px solid rgba(142,166,4,.6)}}.fx{animation:fxk 2s ease-in-out infinite}`),
    I('fontfallback', '字体回退度量', 'Metric-compatible fallback', '换字体不跳版', 'Swapping fonts must not reflow',
      'size-adjust + ascent-override 把回退字体对齐到主字体度量。', 'size-adjust and ascent-override align the fallback metrics.',
      'text', `@keyframes fxk{0%,49%{font-family:var(--fx-mono,monospace)}50%,100%{font-family:var(--fx-sans,system-ui)}}.fx{animation:fxk 3.4s steps(1,end) infinite}`),
    I('fontswap', '字体加载切换', 'font-display swap', '先看得见，再变漂亮', 'Readable first, refined second',
      'font-display:swap 配度量兼容回退，CLS 才为 0。', 'font-display: swap plus metric-compatible fallbacks keeps CLS at zero.',
      'text', `@keyframes fxk{0%,45%{font-family:var(--fx-mono,monospace);letter-spacing:0}55%,100%{font-family:var(--fx-sans,system-ui);letter-spacing:-.03em}}.fx{animation:fxk 4s steps(1,end) infinite}`),
    I('subsetload', '按需子集', 'Subset by need', '首屏只下那几十个字形', 'Ship only the glyphs above the fold',
      'unicode-range 分片 + 中文按字频分包。', 'Split by unicode-range; chunk CJK by frequency.',
      'text', `@keyframes fxk{from{opacity:.25}to{opacity:1}}.fx>span{animation:fxk .5s linear infinite alternate;animation-delay:calc(var(--i)*80ms)}`),
    I('inktrap', '墨陷补偿', 'Ink trap', '小字号笔画交叉处要留缝', 'Tiny type needs relief at the joins',
      '选带 ink trap 的字体，或用 opsz 轴代劳。', 'Pick a face with ink traps, or let the opsz axis handle it.',
      'text', `@keyframes fxk{0%,100%{font-size:clamp(14px,2.2vw,18px)}50%{font-size:clamp(44px,8vw,86px)}}.fx{animation:fxk 5s ${E} infinite}`),
    I('typescale', '模块化字阶', 'Modular scale', '字号来自一条比例', 'Sizes come from one ratio',
      '1.25 或 1.333 的等比数列，只用其中 5–6 级。', 'A 1.25 or 1.333 ratio — use only five or six steps.',
      'list', `@keyframes fxk{from{opacity:.4;transform:translateY(6px)}to{opacity:1;transform:none}}.fxcol{gap:6px}.fx{height:auto;padding:6px 14px;font-size:calc(11px + var(--i)*3px);background:transparent;border:0;animation:fxk .6s ${EXPO} infinite alternate;animation-delay:calc(var(--i)*70ms)}`),
    I('rhythm', '垂直节奏', 'Vertical rhythm', '段距是行高的整数倍', 'Spacing in multiples of the leading',
      '所有纵向间距取 line-height 的 0.5 / 1 / 1.5 倍。', 'All vertical space at 0.5×, 1× or 1.5× the leading.',
      'list', `@keyframes fxk{0%,100%{gap:10px}50%{gap:24px}}.fxcol{animation:fxk 4.4s ${E} infinite}`),
    I('capalign', '视觉对齐', 'Cap-height alignment', '图标要对齐字的高度而非行盒', 'Icons align to cap height, not the line box',
      '按 cap-height 计算偏移，别用 vertical-align:middle 敷衍。', 'Offset by cap-height instead of vertical-align: middle.',
      'list', `@keyframes fxk{0%,100%{transform:translateY(0)}50%{transform:translateY(-2px)}}.fx>i{animation:fxk 3s ease-in-out infinite}`),
    I('numeralwidth', '数字宽度过渡', 'Digit width shift', '位数变化时不推挤后面', 'Adding a digit must not shove the rest',
      '数字容器给 min-width:Nch 并右对齐。', 'Give the number min-width in ch and right-align it.',
      'num', `@keyframes fxk{0%,100%{min-width:5ch}50%{min-width:7ch}}.fx{display:inline-flex;justify-content:flex-end;font-variant-numeric:tabular-nums;animation:fxk 3.4s ${E} infinite}`),
    I('ellipsismiddle', '中间省略', 'Middle ellipsis', '文件名要保住后缀', 'Keep the extension visible',
      '按字符宽度从中间裁，别用 text-overflow 一刀切尾。', 'Trim from the middle by measured width, not text-overflow.',
      'list', `@keyframes fxk{0%,100%{max-width:100%}50%{max-width:52%}}.fx{white-space:nowrap;overflow:hidden;text-overflow:ellipsis;animation:fxk 4s ${E} infinite;animation-delay:calc(var(--i)*-140ms)}`),
    I('lineclamp', '多行截断', 'Line clamp', '第三行末尾接省略号', 'Ellipsis at the end of line three',
      '-webkit-line-clamp:3 配 overflow:hidden，行高必须固定。', '-webkit-line-clamp: 3 with a fixed line-height.',
      'text', `@keyframes fxk{0%,100%{-webkit-line-clamp:3}50%{-webkit-line-clamp:2}}.fx{font-size:clamp(18px,2.8vw,24px);font-weight:400;white-space:normal;max-width:16ch;display:-webkit-box;-webkit-box-orient:vertical;overflow:hidden;animation:fxk 4.4s steps(1,end) infinite}`),
    I('softhyphen', '软连字符', 'Soft hyphen', '只在需要时才断', 'It only breaks when it must',
      '在长复合词里插 &shy;，比 hyphens:auto 更可控。', 'Insert soft hyphens in long compounds for precise control.',
      'text', `@keyframes fxk{0%,100%{max-width:9ch}50%{max-width:6.5ch}}.fx{font-size:clamp(24px,4vw,38px);white-space:normal;hyphens:manual;animation:fxk 4.6s ${E} infinite}`),
    I('nbsp', '不断行空格', 'Non-breaking space', '数字和单位不能被拆开', 'Never split a number from its unit',
      '3&nbsp;425&nbsp;ha —— 单位、姓名缩写都该用 nbsp。', 'Use nbsp between value and unit, and after initials.',
      'num', `@keyframes fxk{0%,100%{letter-spacing:0}50%{letter-spacing:.06em}}.fx{white-space:nowrap;animation:fxk 4s ease-in-out infinite}`),
    I('quotecurl', '弯引号', 'Curly quotes', '直引号是错的', 'Straight quotes are a bug',
      '排版时把直引号替换成 “ ” ‘ ’，代码块除外。', 'Replace straight quotes with typographic ones — outside code.',
      'text', `@keyframes fxk{0%,49%{transform:skewX(0)}50%,100%{transform:skewX(-8deg)}}.fx>span:first-child{display:inline-block;animation:fxk 3s steps(1,end) infinite}`),
    I('dashes', '破折号分工', 'Dash discipline', '连字号、短破折、长破折各有其用', 'Hyphen, en dash and em dash each have a job',
      '-连词、–范围、—插入语，中文用——占两格。', 'Hyphen joins, en dash ranges, em dash interrupts.',
      'text', `@keyframes fxk{0%,100%{letter-spacing:-.02em}50%{letter-spacing:.04em}}.fx{animation:fxk 4s ease-in-out infinite}`),
    I('printpagination', '分页排版', 'Paginated layout', '按纸张高度切页', 'Break by paper height',
      'break-inside:avoid 保护卡片，标题后禁止断页。', 'break-inside: avoid on cards; no break right after a heading.',
      'scroll', `@keyframes fxk{0%,100%{box-shadow:0 0 0 1px #e4e5e0}50%{box-shadow:0 0 0 1px #e5a68f}}.fx{height:150px;background:#fff;animation:fxk 3.4s ease-in-out infinite;animation-delay:calc(var(--i)*-160ms)}`),
    I('layoutdiff', '排版差异高亮', 'Layout diff', '改了哪一行看得见', 'You can see which line moved',
      '两次断行结果做 diff，只给变化的行加过渡。', 'Diff the two break solutions and transition only the changed lines.',
      'list', `@keyframes fxk{0%,100%{background:#fcfcfb;transform:translateX(0)}50%{background:#f4f6ee;transform:translateX(6px)}}.fx:nth-child(2),.fx:nth-child(5){animation:fxk 3s ${E} infinite}`)
  ]
};
