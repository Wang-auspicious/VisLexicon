# VisLexicon（视元）完整交接文件

**日期：** 2026-08-31  
**工作区：** `D:\Desktop\VisLexicon  视元`  
**状态：** 用户因额度用尽要求停止开发并生成交接；当前没有运行中的子代理  
**重要：** 根目录不是 Git 仓库，不能依赖 `git diff`、commit 或 worktree 还原上下文

---

## 0. 一屏结论

今天完成了三条主要工作线：

1. 把策展页从方向原型修到真实 Oreo 几何，并保留 6 个三截图人工核验站点。
2. 建立 3,229 个网站的完整候选目录，接入 100 条/页、33 页和多维筛选 UI。
3. 建立 419 条真实可视/交互 Visual Atlas 数据；另有 194 条 AI Interaction Atlas 覆盖维度，明确不计入图鉴数量。

最后进入“中立图鉴发现首页”实现：

- **Task 1 已完成：** Visual Atlas 薄索引与 419 个逐记录证据端点。
- **Task 2 尚未开始写文件：** `atlas-catalog.js` 与对应测试都不存在。
- 用户要求停止后，Task 2 代理已立即中断；当前不是半截实现。

最新验证：

- `node --test tests/*.test.mjs`：**109/109 通过**。
- `npm run lint`：退出码 0；仅保留 `demos.jsx`、`variants.jsx` 的既有警告。
- `npm run build`：成功。
- 当前 Vite 开发服务器仍监听 `127.0.0.1:5173`，PID 18000；后续任务开始时先重新检查，不要假设它一直存在。

---

## 1. 产品事实基线

VisLexicon 不是单一 Wiki，也不是另一个组件搬运站。

它同时服务：

- 人类：设计选型、专业命名、视觉图鉴、优质来源发现。
- Agent：受控词表、Design Spec、JSON/llms.txt、证据与可解引用上下文。

四个主频道保持独立：

1. **策展**：精选外部设计站与大规模候选目录。
2. **词典 / 图鉴**：正式可运行词条、Visual Atlas Candidates、未来 Scene Anatomy。
3. **工具**：视觉测量、Diff、Spec 提取等专业工作台。
4. **提交**：诚实的本地草稿/导出流程；没有后端时不伪称已入审核队列。

根领域词汇已更新在 `CONTEXT.md`：

- Design Phenomenon：跨媒介可识别的设计现象。
- Medium Binding：同一现象在 Web、Figma、PPT、AE 等媒介中的实现名称。
- Visual Atlas Record：有来源证据的图鉴记录，不等于完成的本地 demo。
- Published Lexeme：具有稳定 ID、本地可运行标本与完整编辑证据的正式词条。
- Atlas Candidate：有来源证据但仍待编辑/本地实现的图鉴候选。
- Scene Anatomy：按空间位置帮助用户发现名称的一种方法。
- Coverage Dimension：用于检查任务、数据、约束、渠道覆盖的非视觉 taxonomy，永不计入图鉴条数。

---

## 2. 本次任务从哪里接手

用户要求读取并接续 Codex 任务：

`codex://threads/01a052ee-ae66-70e0-9bd0-7b992bead5ee`

旧任务标题：`整理最新对话并开始制作`。

旧任务中已经发生的重要事情：

- 从 Claude Code 本地历史整理出 `Claude-最新完整对话整理.md`。
- 修复 Store、Palette、Spec、Diff、词典完整性、提交文案等功能问题。
- 顶栏曾严重偏离 Oreo，用户明确要求“一个像素都不要不同”。
- 后续独立官网审计与实现代理已经把顶栏收口。
- 最后一轮停在大规模站点采集与重新对照 Claude 需求。

本次任务开始时，用户已经重启 Codex 并启用 1M 上下文。

本机当前配置：

```toml
model_context_window = 1000000
model_auto_compact_token_limit = 900000
```

备份：

- `C:\Users\zjz65\.codex\config.toml.before-1m-20260831-094454.bak`
- `C:\Users\zjz65\.codex\config.toml.bak`

无需再次修改 1M 配置。

---

## 3. 今天的探索、错误与推翻过程

### 3.1 读取旧任务与恢复事实

完整读取了旧 Codex 任务、Claude 整理、正式设计规格和实施计划。

确认：

- 顶栏已经完成，不应继续反复改动。
- 旧测试虽锁了 Oreo 变量，但策展页实际几何仍不符合变量。
- 站点采集代理在旧任务中启动，但没有形成可导入产物。

### 3.2 策展页真实几何修复

浏览器实际测量的旧状态：

- 白底板宽 1225px，而不是 1174px。
- 卡片约 383.66 × 319.74px，而不是 380 × 330px。
- 网格间距 14px，而不是列 9px / 行 8px。
- 页面背景仍是 `#f7f7f5`，不是 `#f9f9f8`。
- 截图区还在用 radial mask，而正式规格要求定向渐隐。
- 标题、搜索、技术栈和所有分类挤在白底板顶部，第一屏非常拥挤。

按 TDD 改成：

- 页面背景 `#f9f9f8`。
- 标题/筛选位于外层温润画布。
- 白底板只承载卡片网格。
- 1280px：底板 1174px、圆角 32px、内距 8px。
- 卡片 380 × 330px、圆角 24px、边框 `#e4e4e4`。
- 列距 9px、行距 8px。
- 149px 右渐隐、171px 下渐隐，radial mask 清除。
- 768px 双列；390/320px 单列，无横向溢出。
- 主题标签常驻滚动、hover/focus 暂停；reduced motion 可手动横向查看。
- 首屏前三张主图 eager + high priority，其余图片 lazy。

视觉证据：

- `docs/verification/images/curation-1280x900.png`
- `docs/verification/images/curation-768x1024.png`
- `docs/verification/images/curation-390x844.png`
- `docs/verification/images/catalog-390x844.png`

验收记录：

`docs/verification/2026-08-31-vislexicon-continuation-acceptance.md`

### 3.3 大规模网站目录：从 28 条误抽样到 3,229 条

旧文件状态：

- `toools-extracted.json` 只有 28 条，不能代表 Toools 全量。
- `parsed-design-sites.json` 约 1087 条旧英文目录，未产品化。

新采集线：

- Toools.design 官方 18/18 分类页：2333 原始记录，规范化后覆盖 2305 个 unique entries。
- `bradtraversy/design-resources-for-developers` 固定 commit：1088 原始记录，覆盖 1080 个 unique entries。
- 两源合并 3421 原始行，最终 3229 unique candidates。
- 合并去重 192 行：同源重复 36、跨源重合 156。
- 140 个短链已解析；4 个无法确认的联盟跳转显式标记 unresolved。
- 3224 条中文简介来自逐条机器翻译；5 条使用保守 taxonomy 摘要。
- 原英文定义和来源证据全部保留。

9 个一级分类：

- 视觉素材与字体 754
- 设计创作与原型 577
- UX 研究与学习 477
- UI 组件与设计系统 352
- AI 设计工具 275
- 灵感与案例 235
- 协作与效率 207
- 品牌与营销 184
- 前端开发与动效 168

还有：

- 48 个细分类。
- 33 个搜索标签。
- 价格模型和来源维度。

主要文件：

- `demo/src/data/site-catalog.json`：5,230,742 bytes，完整证据。
- `demo/public/data/site-catalog-index.json`：1,330,874 bytes，浏览器精简索引。
- `demo/scripts/collect-toools.mjs`
- `demo/scripts/collect-design-resources.mjs`
- `demo/scripts/translate-site-descriptions.mjs`
- `demo/scripts/build-site-catalog.mjs`
- `demo/tests/site-catalog.test.mjs`
- `demo/docs/site-catalog-collection.md`

### 3.4 网站目录 UI：用户否决“30 条太少”

第一版目录 UI 只渲染 30 条，再点“显示更多”。

虽然所有 3229 条都已在数据中，但这种界面让用户感觉只收录了 30 条。

用户明确纠正：至少收录 1000+，并需要详细分类。

最终改成：

- 100 条/页。
- 共 33 页。
- 顶部和底部分页、页码跳转。
- 一级分类 + 细分类 + 标签 + 价格 + 来源 + 全文搜索。
- 切换一级分类时清掉失效的细类/标签/价格/来源状态，避免“看不见的筛选条件”。
- 首次加载完成后焦点进入目录搜索框。
- 翻页后焦点和视口回到新结果顶部，并避开 60/80px 固定顶栏。
- 搜索文本加载时一次性规范化；查询 benchmark：median 约 0.69ms，p95 约 1.04ms。
- 100 条 DOM 窗口仅为分页大小，不再被呈现为收录规模。

主要文件：

- `demo/src/SiteCatalog.jsx`
- `demo/src/lib/site-catalog-browser.js`
- `demo/tests/catalog-browser.test.mjs`

### 3.5 采集/构建管线的真实性修复

代码审查发现：

- GitHub 网络请求失败时，旧脚本会静默回退到旧 JSON、写入默认输出并退出 0。
- 构建器曾先覆盖文件再校验。

已修复：

- 在线采集失败直接非零退出并保留旧文件。
- 只有显式 `--offline` 才生成带 `failures` 的离线数据。
- catalog validator 拒绝任何 source failures、非 `candidate` 或非 `directory-listing` 状态。
- 构建器先校验再写临时文件，并逐文件 rename。
- 临时原子写验证成功。

### 3.6 恢复 Claude 昨天最后一条未完成检索

读取的最新本地历史：

`C:\Users\zjz65\.claude\projects\D--Desktop-VisLexicon----\48bd4899-e902-4615-8876-a84e5942b49c.jsonl`

这条对话最后因额度 402 中断。

Claude 当时已找到：

- `https://uianatomy.dev/`
- `https://namethatui.com/`
- `https://motion-vocabulary.vercel.app/`
- `https://vocabulary.vikingz.me/`
- `https://component.gallery/components/`
- `https://www.uiguideline.com/`
- `https://animations.dev/vocabulary`

关键思想：

- 用户往往知道东西在什么位置，却不知道名称。
- “场景解剖图”可以帮助用户从空间位置找到专业名。
- Agent GUI / Prompt Composer 只是 Claude 用来说明方法的一个例子。

后续真正的研究报告：

- `docs/research/2026-08-31-visual-atlas-400-sources.md`
- `docs/research/2026-08-31-visual-atlas-source-candidates.json`
- `docs/research/2026-08-31-visual-atlas-taxonomy-audit.draft.md`

### 3.7 Visual Atlas 第一版：技术上 477 条，语义上被推翻

第一版数据源：

- animations.dev 91
- assistant-ui 120
- GOV.UK 72
- AI Interaction Atlas 194

得到 477 条记录并通过技术测试。

独立 taxonomy 审计指出根本错误：

- AI Interaction Atlas 官方明确说自己不是 UI framework。
- 194 条由 AI tasks、human actions、system operations、data types、constraints、touchpoints 构成。
- 它们主要是覆盖矩阵，不是可观察视觉词条。
- 把它们计入“400 个视觉图鉴”是在凑数。

因此第一版 477 条被否决。

新增领域概念：Coverage Dimension。

### 3.8 Visual Atlas 修订版：419 个真实可视/交互记录

最终 counted sources：

- animations.dev：91，MIT
- assistant-ui：120，MIT
- GOV.UK Design System：72，MIT
- Vercel AI Elements：48，Apache-2.0
- Prompt Kit UI items：21，MIT；排除 2 个 composed recipes
- Loquix custom elements：53，MIT
- WAI-ARIA APG：30，W3C Software and Document License
- Open UI：31，W3C Software and Document License

总输入 466 条。

精确碰撞合并 47 条。

最终：

- 419 counted unique Visual Atlas Records。
- 417 Candidate。
- 2 个与现有 Published Lexeme 精确匹配，带 `localLexemeId`。
- 194 个 AI Interaction Atlas records 全部放在 `coverageDimensions`。
- `coverageDimensions[].countedAtlas = false`。
- AI Interaction Atlas 进入 counted entries 的数量为 0。
- 1274 条机器翻译缓存，失败 0。
- animations.dev 的 Moro / Vikingz / motion-vocabulary 衍生展示不重复计数。

主要文件：

- `demo/src/data/visual-atlas.json`：1,136,219 bytes。
- `demo/data/visual-atlas-translations.zh.json`
- `demo/data/visual-atlas-sources/*.raw.json`
- `demo/scripts/collect-visual-atlas.mjs`
- `demo/scripts/translate-visual-atlas.mjs`
- `demo/scripts/build-visual-atlas.mjs`
- `demo/scripts/visual-atlas/normalize.mjs`
- `demo/scripts/visual-atlas/source-parsers.mjs`
- `demo/tests/visual-atlas-data.test.mjs`
- `demo/tests/visual-atlas-parsers.test.mjs`

数据规格：

`docs/superpowers/specs/2026-08-31-visual-atlas-data-pipeline-design.md`

数据实施计划：

`docs/superpowers/plans/2026-08-31-visual-atlas-data-pipeline.md`

### 3.9 错误 UI 方向：把 Agent GUI 当默认页

曾经提出：词典默认打开 Agent GUI Scene Anatomy。

用户明确否决：

- Agent GUI 只是众多设计需求中的一个。
- 只有正在搭建 Agent GUI 的人才会找这个场景。
- 不能让一个局部例子绑架整个图鉴默认架构。

这条错误规格已永久标记 rejected：

`docs/superpowers/specs/2026-08-31-visual-atlas-discovery-ui-design.md`

禁止后续代理重新实现它。

### 3.10 正确 UI 架构：中立发现首页

用户随后说：“那你要知道怎么改，然后改掉。”

据此批准的正确方向：

- `#/lexicon` 不默认任何场景、媒介、词条或标本。
- 默认页只问：“你现在知道什么？”
- 四种中立入口：
  1. 知道名称或能描述。
  2. 知道自己在做什么。
  3. 知道它属于哪类现象。
  4. 想系统补全见识。
- Agent GUI 只嵌套在 `Digital product interfaces` 场景下。
- Scene Anatomy 是一种方法，不是默认内容。
- 第一实现切片暂不做任何 Scene Anatomy 图；先把中立入口、统一检索和 Candidate 证据路由做对。

批准规格：

`docs/superpowers/specs/2026-08-31-visual-atlas-neutral-discovery-design.md`

完整实施计划：

`docs/superpowers/plans/2026-08-31-visual-atlas-neutral-discovery.md`

---

## 4. 当前已实现的成品能力

### 4.1 Oreo 顶栏

桌面：

- 80px 高。
- 36px 横向内距。
- 品牌后 48px。
- 导航间距 36px。
- Inter Variable。
- 移动断点 768px。

移动：

- 60px 高。
- 20px 横向内距。
- 36 × 24px hamburger。
- 20 × 1.5px 双线。
- 展开菜单几何与 Oreo 审计值一致。

产品语义修复：当前策展导航已正确 active；品牌不再独占 `aria-current`。

### 4.2 策展精选层

当前正式显示 6 个站点，每站 3 张独立真实截图：

- Magic UI
- Coss UI
- Hover.dev
- shadcn/ui
- Uiverse
- 21st.dev

Aceternity UI 和 Animata 因人工视觉审查不合格仍未发布。

整卡直达源站，没有中间详情页或“访问源站”按钮。

### 4.3 完整网站目录

- 3229 candidates。
- 100 条/页。
- 33 页。
- 一级分类、细分类、标签、价格、来源、全文查询交叉筛选。
- Candidate / directory-listing 边界明确。
- 完整 5.23MB 证据文件不进入前端 bundle。

### 4.4 现有正式词典

- 62 个可运行 Published Lexemes。
- Layout 10。
- Interaction 12。
- Aesthetic 12。
- Motion 12。
- Component 13。
- Toolkit appendix 3。

当前 `#/lexicon` 仍是旧界面：

- 左侧文件树。
- 默认选中 Bento Grid。
- 右侧运行 DemoFrame。

**这仍是待改问题。中立首页尚未实现。**

### 4.5 Atlas 静态端点（Neutral Discovery Task 1）

Task 1 已完整实现并经过规格审查 + 质量审查。

产物：

- `demo/public/data/visual-atlas-index.json`：283,834 bytes。
- `demo/public/atlas/atlas-*.json`：419 文件，共 608,553 bytes。

薄索引：

- 419 total entries。
- 417 Candidate。
- 2 Published matches。
- 无 `sourceEvidence`。
- 无 194 Coverage Dimensions。

逐记录 endpoint：

- 每个文件与 `visual-atlas.json` 对应 entry 完整对象 deep-equal。
- 含完整 sourceEvidence。

发布协议：

- validate 成功后才写。
- 详情 → 清 stale → 完整 Atlas → index commit marker。
- stale 只删除 `atlas-*.json`，保留其他 JSON。
- 输出命名空间碰撞会提前拒绝。
- 临时文件使用 PID + UUID，失败不先删除旧目标。
- 同输入连续构建 421 个输出字节一致。

已知非目标：没有跨进程全局构建锁；两个进程同时发布同一路径时，任一失败会保留旧目标并抛错。

---

## 5. 当前精确截断点

实施计划：

`docs/superpowers/plans/2026-08-31-visual-atlas-neutral-discovery.md`

进度：

- Task 1：完成并复审 Approve。
- Task 2：用户要求停止时刚派出实现代理。
- Task 2 代理已在写文件前被中断。
- `demo/src/lib/atlas-catalog.js`：不存在。
- `demo/tests/atlas-catalog.test.mjs`：不存在。
- Task 3–9：全部未开始。

因此下一个代理必须从 **Task 2 的 RED 测试** 开始，不要尝试“接着一份半成品 catalog module 修”。

---

## 6. 后续应该怎么做

严格按实施计划顺序：

### Task 2：统一 Atlas catalog 深模块

创建：

- `demo/src/lib/atlas-catalog.js`
- `demo/tests/atlas-catalog.test.mjs`

先写失败测试，锁定：

- Published 62。
- unmatched Candidates 417。
- total unique results 479。
- 2 个 `localLexemeId` 完全折叠，不重复显示。
- exact/prefix/all-token/definition 排名。
- status/axis/type/domain/scene/source 交叉筛选。
- 24 条分页。
- 输入不可变。
- query benchmark p95 < 5ms。

### Task 3：URL 查询状态和 hash router

创建：

- `demo/src/lib/atlas-route-state.js`
- `demo/tests/atlas-route-state.test.mjs`

修改 `router.js`，正确解析 hash path 和 query；不能破坏默认策展路由。

### Task 4：中立设计领域 / 场景数据

创建：

- `demo/src/data/design-domains.js`
- `demo/tests/design-domains.test.mjs`

根级 8 个 broad domains，Agent GUI 只能位于 `digital-product` 内部。

### Task 5：下沉现有 62 词条浏览器

创建：

- `demo/src/components/lexicon/PublishedLexicon.jsx`

把当前 Lexicon 旧文件树和 DemoFrame 移进去，只有 `mode=published` 才挂载。

`Lexicon.jsx` 变成 orchestrator。

默认不再 `useState(ORDER_IDS[0])`。

### Task 6：中立 DiscoveryHub

创建：

- `demo/src/components/lexicon/DiscoveryHub.jsx`
- `demo/src/styles/lexicon-atlas.css`

默认页必须：

- 没有 Agent GUI。
- 没有 Bento Grid。
- 没有 selected scene。
- 没有 DemoFrame。
- 有四个中立入口。
- 显示 `62 个可运行正式词条` 和 `417 个来源图鉴候选`。

### Task 7：Atlas Explorer

创建：

- `demo/src/components/lexicon/AtlasExplorer.jsx`

从 `/data/visual-atlas-index.json` 加载薄索引。

Published 和 Candidate 分组、24/页、五类 facets、URL 状态、空结果不保留旧标本。

### Task 8：Candidate 证据页

创建：

- `demo/src/views/AtlasRecord.jsx`

新增 `#/atlas/:recordId`。

只显示证据、机译标记、来源、许可、revision；禁止 DemoFrame、Spec、board、Agent delivery。

### Task 9：浏览器和全量验收

要求：

- 1280 / 390 截图。
- 390 首屏必须是搜索 + 中立入口，而不是旧目录树。
- Agent GUI 只在用户主动展开 Digital product interfaces 后出现。
- 全量 tests、lint、build。
- 更新 acceptance 文档。

---

## 7. 下一代理禁止做的事情

- 不要实现 rejected 的 Agent GUI 默认页。
- 不要默认打开任意场景、词条或 demo。
- 不要把 194 Coverage Dimensions 计入 Visual Atlas 数量。
- 不要把 62 Published + 417 Candidate 宣传成 479 个“完成词条”。
- 不要把完整 `visual-atlas.json` import 到 React bundle。
- 不要把 419 条候选塞进现有左侧文件树。
- 不要给 Candidate 伪造截图、动画、代码、参数或 Spec 操作。
- 不要重做 Oreo 顶栏、策展卡片或 `IndexView.jsx`。
- 不要顺手重做 `Entry.jsx`。
- 不要重新跑全网调研；来源研究已完成。
- 不要重复采集 Toools；3229 数据已完成。
- 不要重新修改 1M Codex 配置。

---

## 8. 已批准与已否决文档

### 已批准 / 当前事实

- `Claude-最新完整对话整理.md`
- `docs/superpowers/specs/2026-08-30-vislexicon-full-site-redesign-design.md`
- `docs/superpowers/plans/2026-08-30-vislexicon-full-site-redesign.md`
- `docs/superpowers/specs/2026-08-31-visual-atlas-data-pipeline-design.md`
- `docs/superpowers/plans/2026-08-31-visual-atlas-data-pipeline.md`
- `docs/superpowers/specs/2026-08-31-visual-atlas-neutral-discovery-design.md`
- `docs/superpowers/plans/2026-08-31-visual-atlas-neutral-discovery.md`

### 明确否决

- `docs/superpowers/specs/2026-08-31-visual-atlas-discovery-ui-design.md`

该文件已标记 `rejected — do not implement`，原因是错误地把 Agent GUI 设为默认入口。

---

## 9. 研究来源与口径

Visual Atlas counted 只使用 8 个许可清楚的一手可视/交互来源。

来源细节、commit、许可、计数和衍生关系全部在：

`docs/research/2026-08-31-visual-atlas-400-sources.md`

重要计数口径：

- animations.dev canonical vocabulary 是 91。
- motion-vocabulary、Vikingz、Moro 都是衍生展示，新增计数为 0。
- assistant-ui 的 docs / primitive docs / registry 是同一生态的不同投影，不能简单相加。
- AI Elements live catalog 用 48，不用仓库未上线的第 49 条。
- Prompt Kit 排除 2 个 composite recipes。
- APG 是 30，不是 31。
- UI Anatomy、NameThatUI、UI Guideline 等无明确开放许可来源仅作参考/链接，不批量复制原文和素材。

---

## 10. 当前测试与构建证据

最后一次独立运行：

```text
node --test tests/*.test.mjs
tests 109
pass 109
fail 0
duration ≈ 5.6s
```

Lint：

```text
npm run lint
exit 0
```

现存警告全部来自旧文件：

- `src/demos.jsx`
- `src/variants.jsx`

主要为 React Fast Refresh only-export-components；还有既有 set-state-in-effect 和 constant-nullish warnings。

本轮新改文件定向 lint 已多次跑到零警告。

生产构建：

```text
vite v8.2.2
48 modules transformed
dist/index.html                 0.86 kB / gzip 0.57 kB
dist/assets/index-*.css       161.85 kB / gzip 33.83 kB
dist/assets/index-*.js        404.63 kB / gzip 136.57 kB
build success
```

Atlas Task 1：

```text
npm run atlas:test
27/27 pass
```

Task 1 复审：

- 规格审查：Approve。
- 质量审查初次：With fixes。
- 修复后质量复审：Approve。

---

## 11. 已知风险与刻意保留的权衡

### 11.1 Oreo 非活跃导航对比度

为保持 Oreo 实测像素，非活跃桌面导航使用黑色 `opacity: .4`，在 `#f9f9f8` 上约 2.83:1，低于 WCAG 4.5:1。

移动端当前项有 `aria-current`，但按 Oreo 实测没有额外视觉样式。

这是已知、明确的“像素复刻优先”权衡，不能宣称全站导航完全满足 WCAG。

### 11.2 根目录无 Git

- 不能用 commit/branch/worktree 证明变更。
- 不要执行 `git reset` 等命令。
- 交接依赖测试、文件内容、修改时间、生成产物和本文件。

### 11.3 Atlas 多进程构建锁

当前单构建路径安全，临时名不会碰撞；没有跨进程全局锁。

两个进程同时发布相同最终路径时，失败者会保留旧目标并抛错，不会悄悄成功。

### 11.4 PUA skill 安装不完整

PUA 主 `SKILL.md` 可读，但它强制引用的：

- `display-protocol.md`
- `methodology-router.md`
- `flavors.md`
- `methodology-*.md`

在 `.agents/skills` 和 `.codex/skills` 中均不存在。

错误已记录在 `.learnings/ERRORS.md`；不要伪造这些文档的内容。

### 11.5 系统临时目录残留

原子写测试曾生成：

`C:\Users\zjz65\AppData\Local\Temp\vislexicon-catalog-atomic-check`

本机安全策略阻止自动清理；它只包含测试生成的 full/index JSON，不影响工作区。

---

## 12. 下一代理的最短启动路径

不要重新读整份 Claude/Codex 历史。

按顺序读：

1. 本文件。
2. `CONTEXT.md`。
3. `docs/superpowers/specs/2026-08-31-visual-atlas-neutral-discovery-design.md`。
4. `docs/superpowers/plans/2026-08-31-visual-atlas-neutral-discovery.md`。
5. `demo/src/entries.js`。
6. `demo/src/data/visual-atlas.json` 的顶层 schema 和少量样本；不要整份塞进上下文。
7. `demo/public/data/visual-atlas-index.json` 的 schema 和少量样本。

然后从 Task 2 开始：

```powershell
cd 'D:\Desktop\VisLexicon  视元\demo'
node --test tests/*.test.mjs
```

预期基线：109/109。

第一步只创建 `tests/atlas-catalog.test.mjs`，运行到预期 RED；在看到 RED 前不要写 `src/lib/atlas-catalog.js`。

---

## 13. 最后一句交接

当前数据、策展、网站目录和 Atlas 静态端点已经完成且全绿。

真正未完成的是：把旧词典的“默认 Bento Grid + 文件树”改成用户批准的中立发现首页，并将 62 Published Lexemes 与 417 unmatched Candidates 通过统一 catalog 安全接入。

不要再让 Agent GUI、Bento Grid 或任何一个局部例子成为默认答案。

