# VisLexicon 竞品调研与实现思路完善

> 调研时间:2026-08-28
> 基于:《VisLexicon-完整方案.md》(定位与 L1-L4 架构)《rebuttal.md》(收录模式/词典形态/测量工具/呈现的修正)
> 结论先行:**产品定位成立,空白点真实存在。方向不是再做一个画廊,而是把「词表 + 结构化 Schema + Agent 协议」做成别人没做成的样子。能直接拿来用的开源件也明确了——Registry 格式、Uiverse 数据源、设计资源列表,全部可复用,不重复造轮子。**

---

## 0. 一句话结论

市面上"把人导向内容"的产品已经卷到天花板(Godly / toools / Land-book / 21st / Mobbin),但**"把视觉内容结构化成 Agent 可消费的受控词表"这个象限基本空白**。VisLexicon 的正确打法不是和画廊比收藏量,而是:

- **内容**用现成开源数据源(Uiverse、设计资源列表、公开 Registry)喂,不自己采;
- **协议**直接兼容 shadcn Registry JSON Schema + shadcn 官方 MCP Server,不重造;
- **差异化**押在词典(Effect Genome + 记谱法 + 检索表)和测量工具上,这两块没人在做。

---

## 1. 竞品形态地图

按"收录内容 → 分类方式 → 分发协议 → 商业模式"四维切分,现有玩家分为五类:

### 1.1 策展型链接目录(Curated Link Directory)

**代表**:toools.design、Land-book、Godly、各种 awesome-list

- **收录**:链接 + 一句话描述 + 截图,跳转外站;
- **分类**:手工标签(tag),无受控词表,标签即"自由文本";
- **协议**:纯网页,若 Agent 要用得靠爬或 llms.txt;
- **商业**:赞助位、会员去广告。

**观察**:这一形态被验证可行,且对作者友好(被收录=免费分发+反链)。rebuttal.md 判断的"外链目录对你这个定位是唯一正确模式"成立。**但这形态没有护城河**——任何人三天能搭一个,且它们对你构不成威胁(你定位是给 Agent 供货,不是给人刷灵感)。

### 1.2 组件注册表(Component Registry)

**代表**:21st.dev(2026 现状)、shadcn/ui Registry 生态、Magic UI、Aceternity、Origin UI

- **收录**:可安装的组件代码(prompt 或 shadcn CLI 安装);
- **分类**:按类型(components/templates/themes/icons)+ 用途路径(`/s/`)+ 作者;
- **协议**:**shadcn Registry JSON Schema 已是事实标准**,且 shadcn 官方在 2025-08 发布了 MCP Server(CLI 3.0 同期)——**Agent 协议在这个生态已经通了**;
- **商业**:21st 每天送 2 次免费拷贝,会员无限拷贝 + 付费模板。Mobbin 之类的订阅制不适用这里。

**观察**:
- 21st 的"组件=可拷贝代码"形态已被吃透,**我们不在这条赛道竞争**。
- 但它的**Registry 格式 + 作者 + 安装量元数据**是我们要复用的基础设施,不是竞争对手。

### 1.3 开源组件/资源集合(Open-Source Collection)

**代表**:uiverse.io(galaxy 仓库)、bradtraversy/design-resources-for-developers

- **uiverse/galaxy**:3000+ UI 元素,纯 CSS/Tailwind,MIT 许可,按类型分目录(buttons/cards/forms/loaders…),**数据是平台自动同步的只读镜像**——适合直接读来索引,不适合在仓库层增补;
- **design-resources-for-developers**:66.8k star,MIT,按类别(UI Components/CSS Animations/Design Systems/…逐项列出名称+链接+一句描述),**程序化解析即可变成结构化索引**。

**观察**:这两个是**冷启动数据源的现成选项**,能直接喂给我们的 L2-B 生态索引。**不用自己爬全网建目录**。

### 1.4 设计参考资料库(Design Reference)

**代表**:Mobbin、60fps.design、Refero、Page Flows

- **Mobbin**(订阅制,Pro 12$/月):收录真实 iOS/Android/Web 应用的截图 + 完整用户流程录像(flow),按应用/界面类型/UI 元素/流程/平台/行业过滤,支持自然语言搜索;**无 API,无 Agent 接口**。移动应用场景强,网页应用覆盖薄。
- **60fps.design**(免费):550+ 条真实 App 交互动效视频,**附带动画术语表(glossary)**,并且**已有 MCP Server**(LobeHub 可查,返回真实 iOS 动效 clip + Swift 文件)。

**观察(关键)**:
- **Mobbin 的形态我们不做**(真实产品截图 + 流程,付费墙,无协议)——它是"设计研究工具",我们既不收钱也不主打"还原真机产品"。
- **60fps 是最接近我们要做「词典」的对手**,但它以视频 clip 为核心、词表是附属(learn/glossary),**没有词表 Schema、没有可解引用的 ID、没有测量工具、没有中英别名**。这恰恰是 VisLexicon 的差异化空间。
- **Agent 协议已有人跑通(60fps MCP / shadcn MCP)**,说明"给 Agent 供给视觉内容"是真实需求且可行——这验证了我们的 L4 层方向,同时要求我们必须**在词表结构化上做到比 60fps 深**。

### 1.5 AI 生成类(Lovable / v0 / 21st AI)

不细展开——它们是"目的地(帮你把页面造出来)",我们是"供给站(告诉 Agent 该长什么样)"。竞争关系弱,反而是我们协议的下游消费者(Agent 会用我们的词表/JSON 喂给这些工具)。**这一层拿到的洞察**:下游 Agent 需要的是**结构化参数,不是 GIF/视频**,这印证了完整方案 §8.2"关键帧 + 参数文字 > mp4"的判断。

---

## 2. 空白在哪(为什么这个产品值得做)

把上面五类的交集挖掉,剩下来的空白:

| 维度 | 现有玩家 | 空白 |
|---|---|---|
| **受控词表** | 60fps 有动效 glossary,但无 Schema、无 ID、无中英别名 | **有 schema 的词表 + i18n 别名映射 + 可解引用 ID** |
| **结构化分类** | 画廊用自由 tag;21st 用类型+作者 | **五轴分类 + 组件级粒度**,让「跨库按 Bento Grid 筛选」成为可能 |
| **Agent 协议** | shadcn MCP(组件)、60fps MCP(视频)、Mobbin 无 | **词典/组件/案例/Spec 的稳定 JSON 端点 + 一站 MCP**,专注于「设计上下文」 |
| **测量工具** | 无 | **Spec 提取器 + Diff 描述器**(测量在前、VLM 在后)——完全空白 |
| **免费 + 开源协议** | 21st 付费、Mobbin 订阅 | **免费 + 数据开源(CC BY-SA)** |

**一句话**:Mobbin 是"设计图的图书馆",21st 是"组件的超市",60fps 是"动效的博物馆"。三者都是**内容型**。VisLexicon 做**结构型**——把内容和词表、协议、测量绑起来。三者的内容我们都能索引,但**结构只有我们有**。

---

## 3. 能直接用的开源件(不重复造轮子)

| 要做的 | 直接用 | 怎么用 |
|---|---|---|
| Agent 协议格式 | **shadcn Registry JSON Schema** | 组件记录直接兼容该 schema,我们的 `.json` 端点即 Registry 子集;Agent 用 `npx shadcn add` 或读取对应字段即可 |
| MCP 服务实现 | **shadcn 官方 MCP Server**(开源,CLI 3.0 同期发布) | 以它为参考实现/样例架构,或在其上做「设计上下文」工具集,而非从零写 MCP 协议 |
| 词典演示代码 | **Magic UI / Aceternity / Origin UI / Hover.dev 等公开组件** | 每个词条的 live demo 复用这些组件源码(注意其 license,多数 MIT 可注明出处),演示和 minimal_code 一体,不自己造 200 个动效 |
| 生态索引冷启动数据 | **uiverse/galaxy**(3000+ 组件, MIT) | 直接读目录/frontmatter 生成组件记录;作为 L2-B 的种子数据 |
| 生态索引补充 | **bradtraversy/design-resources-for-developers**(MIT,66.8k star) | 按 README 类别解析出「名称+URL+一句描述+类别」,程序化转成分级索引;注意剔除赞助商条目 |
| 图标匹配索引 | **Lucide / Heroicons / Phosphor / Tabler** | 作为 Spec 提取器的图标相似检索底座(§rebuttal 3.1.5) |
| 部署/图床/DB | **Vercel/Cloudflare Pages + Cloudflare R2 + Supabase** | 按完整方案 §11.1,不引新服务 |

**明确不自己做的**:不做浏览器 SDK/爬虫内核(用社区),不自研 embedding 模型(SigLIP/CLIP + 多语言文embedding),不手写 MCP 协议层(shadcn 开源实现可直接参考)。

---

## 4. 已有近似产品 vs 我们(逐条结论)

1. **toools / Godly / Land-book**:收录模式被验证 ✓,但它们是"链接堆"。我们叠加 **统一截图层 + 五轴标签层 + Agent 端点层**(rebuttal §1.4),把"元数据中心化、像素去中心化"落到整站收录。
2. **21st.dev**:它的"组件即代码 + 作者 + 安装量"很棒,但商业靠付费。我们**兼容其 Registry 格式**、**免费开放**,在这个格式之上加**词表标签 + Agent 端点**。**兼容不竞争**。
3. **60fps.design**:最接近的"词典"对手,但它视频驱动、无词表 Schema。我们做**词典结构化**(Effect Genome + 记谱法 + 检索表 + 解剖台),把 60fps 的"视频灵感库"变成"可推理的结构化词汇"。
4. **Mobbin**:订阅制、无 Agent 接口、移动端强。我们免费、Agent-first、跨端(侧重 Web/前端组件)。**它恰是我们"不需要成为的样子",却是"引用它做 In the Wild 目击来源"的好伙伴。**
5. **uiverse**:本身就是"开源组件库",我们**索引它**而非做它。它是数据源,不是竞品。
6. **shadcn 生态**:Registry Schema + MCP Server 是**我们要站在其肩膀上的地基**,不是对手。

---

## 5. 实现思路的完善(在完整方案基础上)

结合调研,对原方案做几处修正/补强:

### 5.1 词条 Schema 增加「可解引用 + i18n 别名」为第一公民

完整方案 §3.2 的 YAML Schema 对,但要补两点,否则做不成 Agent 协议:

- **每个词条的 `demo` 直接给 Registry 格式的代码引用**(registry item 字段),让 Agent 能"拿到示例即安装",而不是只给一个演示 URL;
- **`aliases` 扩展为中英双语受控别名**,并作为独立检索索引(中文口语→标准术语映射是全站搜索质量的关键,rebuttal §3.3 提过)。

### 5.2 组件记录直接兼容 Registry(而非自造)

把 §5.2 的元数据 JSON 改成 **Registry schema 超集**:保留 `id/title/author/install`,补充我们独有的 `tags(词表ID)/license_gate/health/raw_links`。这样一条记录两端通吃:人类站内看,Agent 用 shadcn 读。

### 5.3 Phase 0 提前引入「词表→JSON 端点」的最小闭环

原方案路线图 Phase 0 是 150 词条 + demo + 搜索。建议 **Phase 0 就实现每词条的 `.json` 端点 + llms.txt**,因为:

- 它几乎零成本(数据是 YAML,静态生成即可);
- 它让"词典是独立可传播产品"成立——用户可以只贴一个词条的 JSON 给 Agent 就见效;
- 它验证 L4 协议,是最大的差异化,不该排到最后。

### 5.4 demo 优先做「解剖台」而非「发布完整词表」

rebuttal §2.4 的解剖台(scrub / X光 / 缓动曲线同步仪)是词典从"查阅"升维为"品味训练器"的关键,也正是**现有竞品(60fps、21st)都没有的交互形态**。它比"再堆 50 个词条"更有差异化价值。demo 就把这个做出来。

### 5.5 收录策略维持 rebuttal 结论

- **外链为主**,叠加四层(截图/标签/健康巡检/Agent 端点);
- **词典做 Effect Genome**(基因位),不写枚举式词霸——这避免"100 词条到顶"的天花板;
- **像素去中心化**(reader 用户侧取图),版权风险最小化。

---

## 6. 风险与应对(新增,基于竞品观察)

| 风险 | 观察依据 | 应对 |
|---|---|---|
| 60fps 已有 MCP,先发一步 | LobeHub 可见 | 我们押「词表结构化 + 测量工具」,60fps 押「视频灵感」;错位竞争,且它无词表 Schema |
| shadcn Registry Schema 变更 | 官方生态活跃 | 组件记录以 Registry 超集存储、用适配层输出,core 数据不绑定具体 schema 版本 |
| 「词表」被大 VLM 内化 | 模型会越来越懂视觉术语 | 坚守**参数可玩 + 测量工具 + 结构化协议**——这些是模型内化不了的"精确数字"(rebuttal §3.3) |
| 免费档图片带宽 | 完整方案 §4.1 | 截图走 Cloudflare R2(出站免费) |
| 一个人维护成本 | 完整方案 §4.5 | 全部设计(GitHub 承载数据/机器审核/静态化/不做社交)服务"收录量翻十倍人力几乎不变" |

---

## 7. 落地路径(对应你要求的顺序)

1. **调研.md**(本文件):竞品 + 开源复用 + 思路完善 —— 已完成;
2. **demo**(Vite + React + Tailwind):做出「词典首页 + 词条页(含解剖台雏形)+ 每词条 JSON 端点」的最小可玩闭环,验证核心体验;
3. 基于 demo 的骨架长成正式站(接数据源、接 MCP、接 R2/Supabase)。

---

## 8. 参考资料(竞品与开源数据源)

**策展目录**:[toools.design](https://www.toools.design) · [Land-book](https://land-book.com) · [Godly.website](https://godly.website)
**组件注册表**:[21st.dev](https://21st.dev) · [shadcn/ui Registry 文档](https://ui.shadcn.com/docs/registry) · [shadcn CLI 3.0 + MCP 更新](https://ui.shadcn.com/docs/changelog/2025-08-cli-3-mcp) · [shadcn/alpine-registry(示例 registry)](https://github.com/shadcn-ui/alpine-registry)
**开源数据源**:[uiverse/galaxy(3000+ 组件, MIT)](https://github.com/uiverse-io/galaxy) · [bradtraversy/design-resources-for-developers(66.8k star, MIT)](https://github.com/bradtraversy/design-resources-for-developers)
**动效词典/灵感**:[60fps.design](https://60fps.design) · [60fps Glossary](https://60fps.design/learn/glossary) · [60fps MCP (LobeHub)](https://lobehub.com/mcp/60fps-design-plugin)
**设计参考**:[Mobbin](https://mobbin.com)([定价参考](https://www.uiguides.com/tools/mobbin-review))
**可复用组件库**:[Magic UI](https://magicui.design) · [Aceternity UI](https://aceternityui.com) · [Origin UI](https://originui.com)

*调研版本 v1.0 · 2026-08-28*
