[本次评审依据](./VisLexicon-设计评审完整简报-2026-09-01.md)

# VisLexicon 独立设计评审反馈

## 一、直觉判断

结论先说：VisLexicon 目前把最稀缺的能力藏在后台，把最普通的结构放在了前台。

真正稀缺的是对每个网站做真实探索、去重、找证据、辨认术语、判断能拿走什么，并把这些判断交给人和 Agent。现在用户先看到的却仍是频道、分类、筛选、卡片和数量。于是产品的内核像编辑部，门面像资源后台。

如果继续沿着“把 7 类分得更合理、把 59 小类收得更完整、再给搜索框加自然语言”推进，最终只会得到一个会替用户勾选筛选项的目录。它可能比传统导航站准确，却仍然没有形成独特产品。

VisLexicon 最先不该成为“设计网站大全”，也不该成为“词典、策展、工具、提交四件套”。它应该先成为：

> 一张有证据的视觉行动地图：用户描述想做什么或贴出自己看到的东西，系统把真实案例、稳定术语和可用资源组织成一条能判断、能取舍、能交给 Agent 的路线。

第一次进入，用户应立刻明白三件事：

1. 我可以说出正在做的东西，不必先学会这里的分类。
2. 我可以从真实证据判断一个资源值不值得打开。
3. 我挑中的案例和术语可以直接变成一份设计任务说明，而不是收藏后再自己整理。

“收录多少”不应成为第一价值。8684 个候选没有形成 8684 份理解，公开展示这个数字只会放大内容债务。六个做实的条目也不该与数千个低配条目同屏，因为那会告诉用户：这个产品的质量承诺只适用于首页橱窗。

## 二、当前方案的致命问题

### 1. 产品仍以“有什么”为中心，不以“用户下一步做什么”为中心

策展、词典、工具、提交都是产品内部名词。用户不会先想“我该去策展还是词典”，他会想“我要做 Agent 界面”“这个效果叫什么”“这个站值不值得打开”“把这种视觉语言交给我的 Agent”。

替代方案：全站首页以任务和所见为入口；网站、词条、场景、工具只是系统回答时调用的内容形态。只有“系统学习图鉴”是一种明确、独立的阅读意图，可以保留为次级入口。

### 2. 把网站当作最小内容单元，是模型层面的错误

Figma 既是工作工具，也有社区、模板和资源；21st.dev 既是社区，也是组件注册表；Refero 既是参考库，也是分析与导出工具。给每个网站选一个主类，只是为了让表格整齐。

更严重的是，同一网站对不同任务有不同“入口”。用户找协作工具时需要 Figma 编辑器；找模板时需要它的社区资源；研究一个组件命名时，Component Gallery 更像某个词条的证据来源，而不是一张与 Figma 平级的网站卡。

替代方案：把“网站”降为来源实体，把页面、内容单元、术语、证据、任务路线和用户产物建成可连接的对象。前台每次展示的是“这个来源在当前任务里能帮你完成什么”，不是给来源颁发永久身份。

### 3. 所谓智能感目前仍可能只是聊天框包着数据库

自然语言输入本身不智能。真正的智能感来自五个连续动作：

1. 把模糊话语复述成用户能纠正的意图。
2. 只在会改变结果时问一个关键问题。
3. 把不同形态的内容排成一条行动顺序。
4. 解释每个结果为什么出现，以及它不能解决什么。
5. 记住用户的取舍，最后形成可继续使用的产物。

如果输入一句话后仍得到三列同形卡片、标签和一个“相关度”排序，用户只会认为系统做了语义搜索。

### 4. “统一质量”被误写成了“统一卡片”

三张关键图、来源、作者、许可、价格、核验时间是发布标准，不是视觉模板。Kimi 招聘官网、Lucide、Toools 和 Figma 不应被塞进完全一样的 380×330 卡片并用同三行信息解释。技术栈对组件站重要，对品牌官网可能毫无意义；体量对图标库重要，对单一招聘官网则是噪音。

替代方案：统一编辑合同，允许按内容角色采用不同媒体和信息结构。统一的是证据门槛、字段可信度和交互语法，不是每张卡的长相。

### 5. 四个顶层频道切断了本应连续的动作

真实流程可能是：看到 Kimi 招聘站 → 识别它用了哪些排版和动效 → 找相近案例与可复用实现 → 加入设计板 → 生成给 Agent 的说明。现有结构要求用户在策展、词典和工具之间来回跳，并自己保存上下文。

替代方案：顶层只保留“发现”“图鉴”“设计板”。发现是跨内容的默认首页；图鉴服务明确的系统学习与查词；设计板承接选择和输出。“工具”变成对象上的动作，如“分析这个网站”“比较这两个案例”“生成说明”。“提交”降到页尾、用户菜单或馆藏页面，不占据普通访客的核心导航。

### 6. 详情浮窗与直接外跳的反复，暴露的是核心动作未定义

旧决定是整卡直接外跳，最新决定是先打开浮窗。这不是谁新谁旧的问题，而是产品一直没有回答：一次点击要帮用户完成“访问”还是“判断”。

VisLexicon 的独特价值应放在访问之前，因此整卡点击应打开“判断页”；但已经知道要去哪的人不能被强制拦截。卡片上保留清晰、低干扰的“去源站 ↗”文本链接，点击域名或该链接直接新开源站。点击卡片其他区域则打开判断页。这样既尊重最新意图，也不把每次外跳变成流程税。

### 7. 内部生产进度被误当成用户价值

左侧分类进度竖轴解释的是编辑部正在处理什么，不解释用户此刻能得到什么。放在首页会让未完成感盖过内容本身。

替代方案：把它移到独立的“馆藏账本/编辑日志”。普通页面只显示与当前条目有关的核验时间、证据状态和权利状态。过程透明应该发生在证据层，而不是把内部任务看板公开为导航。

## 三、重新定义用户心智

### 1. 用户只有三个入口问题

首页不要让用户选内容类别，而应承接三个自然问题：

- 我正在做什么：描述任务、目标、媒介和限制。
- 我看到了什么：描述现象、贴截图、贴 URL。
- 我想随便逛逛：进入编辑策划的任务路线、近期核验和系统学习。

这三种入口最终进入同一种结果页，只是起点不同。结果页不是“搜索命中”，而是一条可编辑的研究路线。

### 2. 人类与 Agent 不是两套产品

人类负责提出意图、看证据、纠正推断和做取舍；Agent 消费的是同一过程留下的结构化结果。不要做一个“给人看的站”和一个“Agent 入口站”，也不要在首页放一个抽象的 Agent 模式。

正确关系是：

人描述与选择 → 系统保存事实、推断和决策 → 人读简洁说明 → Agent 读取同一份结构化设计板。

未来的 JSON、MCP、llms.txt 或 DESIGN.md 是同一证据模型的不同出口，不是另起一套信息架构。

### 3. 用户不需要理解内容对象，但系统必须分清

底层至少需要六种对象：

| 对象 | 它解决什么 | 例子 |
|---|---|---|
| 来源实体 | 谁提供内容，权利和官方地址是什么 | 21st.dev、Kimi 招聘官网、Lucide |
| 来源内入口 | 用户具体该先去哪里 | 组件目录、某个案例页、编辑器、社区、文档页 |
| 内容单元 | 用户实际能看、学、用或下载什么 | 组件、图标、案例、模板、规范、工具 |
| 视觉词条 | 稳定名称、定义、辨析和可运行演示 | 布局、组件、动效、审美现象 |
| 证据 | 某个判断依据哪个页面、区域或状态 | 三张图、页面区域、授权片段、采样值 |
| 任务路线与设计板 | 当前目标下的组织和用户取舍 | Agent 界面路线、招聘页设计板 |

全局质量分也要拆开。可以保留“来源可信度、内容完整度、核验新鲜度”等基础质量，但“是否有用”只能按当前任务计算。Kimi 招聘官网在“安装 Agent 组件”里不相关，在“做有品牌感的招聘页”里可能是关键案例。

## 四、信息组织：用关系网做底层，用任务路线做前台

### 1. 固定分类树的去留

7 大类和 59 小类可以留在编辑后台做覆盖检查，但不应成为前台主导航，也不应承担唯一归属。后台字段应是互相独立的多维描述：

- 能拿走什么：代码、素材、知识、案例、工具能力、提示词或规则。
- 用户能做什么：安装、复制、下载、比较、学习、创建、测试、分析、欣赏。
- 内容怎样承载：站内统一内容、外链目录、单站作品、社区市场、深度拆解。
- 适用任务：产品界面、Agent、品牌、招聘、动效、三维、字体、演示等。
- 证据深度：概览、完整目录、具体实例、可互动状态、系统分析。
- 使用边界：可复用、只可访问、许可未知、禁止再分发。
- 媒介与实现约束：网页、移动端、框架、文件格式、无障碍、价格。

前台不展示这张字段表。它只用这些字段组合当前任务的路线，并在用户需要精确控制时暴露相关的几项。

### 2. 结果不按“资源类型”固定分组，而按下一步动作组织

系统可以有稳定的内部角色，但前台标题应随任务变化。一次回答通常只出现三到五段，不显示空段，也不机械保持顺序。

例如“我要做带工具调用和审批的 Agent 界面”：

1. 先定结构：消息流、工具调用、来源引用、人类审批等正式词条和场景解剖。
2. 可以直接试：经过核验的组件、仓库或组件站入口。
3. 看真实产品怎样取舍：相关界面和完整产品案例。
4. 补齐细节：图标、代码块、空状态、加载与语音反馈资源。
5. 带走一份任务说明：把用户选中的结构、案例和约束写入设计板。

而“做一个有品牌感的招聘页”应变成：

1. 先看三种叙事策略。
2. 看真实招聘官网的证据。
3. 拆出排版、滚动、语气和动效词汇。
4. 找能实现关键表达的工具或参考。
5. 形成品牌边界和 Do / Don't。

这两条路线不应共享一套固定分组文案。智能感就在于系统知道此刻先解决哪个问题。

### 3. 对混合形态的压力测试

| 对象 | 不应怎样归类 | 在任务路线中的正确角色 | 推荐呈现与动作 |
|---|---|---|---|
| 21st.dev | 不要只叫“组件库”并永久高权重 | 当用户要快速实现时，是可直接试的站内组件注册表；研究社区分发时又是案例 | 显示一个代表性组件预览、覆盖范围和安装/访问入口；三张证据在判断页说明身份、范围和实例；不在 VisLexicon 托管其代码 |
| Toools.design | 不要与它收录的每个目的站混成同一层 | 它是“继续发现外部目的地”的元目录 | 卡片先告诉用户覆盖面、组织方式和适合何时使用；列出三个经独立核验的样本入口。Toools 收录某站不等于 VisLexicon 已核验该站 |
| Refero | 不要只归成灵感馆，也不要把它当作可复制的数据源 | 当用户学习视觉语言时，它是深度拆解参考；当用户要输出说明时，它是流程启发 | 用渐进拆解型判断页，明确第三方来源和不可搬运边界；不复制其视频、文案、数据、模板或完整输出 |
| Kimi 招聘官网 | 不要因不能下载代码而判为低价值“商业展示” | 它是品牌表达和招聘叙事的真实案例 | 以大幅海报或有授权的短动态建立感受，再用三张有标题的证据解释排版、叙事和交互；动作是看原站、加入设计板、查看相关词条 |
| Figma | 不要选一个“工具”主类后丢掉社区与模板 | 它是一个来源实体，下面有编辑器、社区、模板等不同入口；当前任务决定出现哪一个 | 做原型时显示工具能力和进入方式；找模板时显示经核验的社区入口；不要在同一卡片堆完全部身份 |
| Component Gallery | 不要只作为一个普通网站结果 | 它常常是命名、组件结构比较和词条证据源 | 在“这个组件叫什么”里优先作为比较入口；在词条页引用其具体比较位置，并同时给出可运行词条与真实实现 |
| Lucide | 不要只放入“视觉素材”树 | 它是图标语言、可搜索素材、实现资源和系统一致性的交叉点 | 找图标时呈现代表性图标组、获取方式、许可状态和风格特征；做品牌研究时只引用其视觉语言，不展示无关技术栈 |

这套组织方式的关键不是给每个对象贴更多标签，而是允许同一来源在不同任务中以不同“入口”和不同卡片出现，同时仍指向同一个来源事实页。

## 五、首页与发现流程

### 1. 第一屏应该承诺结果，不展示数据库

首页第一屏应只有一个明确承诺、一条主输入和可逛入口。建议主文案：

> 说说你要做什么，或把你看到的东西放进来。

输入框同时接受自然语言；粘贴 URL 和上传截图是输入框内的次级动作，不另做三个同权大按钮。输入框下方用三条真实示例帮助用户理解能力，不用一排泛化风格词胶囊。

第一屏下半部出现一条大幅编辑路线与两条紧凑路线。例如：

- 做一个可信的 Agent 产品界面。
- 看品牌如何把招聘页做成自己的作品。
- 找一套高级但不过度装饰的图标语言。

它们不是永久分类，而是编辑部已经把内容连接好的入口。每条路线必须真的走到案例、词条、资源和设计板，不能只是预填搜索词。

### 2. 不知道搜什么的人如何逛

可逛入口应分为三种编辑视角，而不是 7 个大类：

- 本期路线：围绕一个具体设计任务，讲清先看什么、再做什么。
- 最近核验：只展示刚完成证据复核的正式内容。
- 系统补全：进入图鉴的学习路线，从场景、现象或课程式路径浏览。

Refero 的 Trending、Popular、Newest 可以启发浏览顺序，但 VisLexicon 不应在没有可靠行为数据时制造“热门”。首期用“编辑精选、最近核验、最近新增”更诚实。将来有足够数据后，“常被加入设计板”比单纯点击量更有意义。

### 3. 知道要做什么的人如何得到混合结果

提交输入后，结果页先显示一句“我理解你要做的是……”，并把系统做出的关键假设标出来。用户可以直接改这句话，不需要和聊天机器人来回寒暄。

只有当一个问题会显著改变结果时才追问。例如“你要可安装代码，还是主要找视觉参考？”这是有价值的澄清；“你更喜欢现代还是简约？”通常只是空泛追问。

结果按行动路线组织，每个条目必须带一句当前语境下的推荐理由，例如：

- “包含可直接安装的工具调用卡片。”
- “适合观察审批动作怎样不打断消息流。”
- “这是品牌表达参考，不提供可复用源码。”
- “许可尚未核验，不应直接进入商用实现。”

这句话比十个标签更能证明系统理解了用户。

### 4. 专业控制应该存在，但只在需要时出现

搜索语句中识别出的硬条件直接显示在意图摘要下，例如“React”“必须可商用”“免费”“移动端”。系统没有把握时不要自动锁定。

“调整条件”打开侧边页或底部页，只展示与当前结果有关的字段：

- 找组件时出现框架、安装方式、许可证、价格、无障碍。
- 找图标时出现文件格式、线性/填充、代码包、许可证。
- 找品牌案例时出现行业、页面类型、动效强度、媒介，而不出现 React 下拉框。

每个条件都能设为“必须”“偏好”或“排除”。普通用户不需要先看它们，专业用户也没有失去控制。排序只提供“最符合当前目标”“最近核验”“最近加入”等可解释选项，不再使用永久全局价值分。

## 六、卡片与详情

### 1. 统一内容标准与统一卡片形态必须分开

所有正式网站都应通过同一发布门槛：

- 官方身份、作者/组织、官方入口和核验时间明确。
- 一句人话说明“它是什么、适合谁、能拿走什么”。
- 三份互不重复的关键页面证据，分别说明身份、范围和具体能力。
- 价格、许可证、仓库等信息有来源；未知就明确标未知。
- 当前推荐理由和限制可解释。

但前台至少需要五种内容适配器：

1. 可直接使用型：组件、图标、素材。主媒体展示代表实例，突出能获得什么和使用边界。
2. 参考案例型：品牌官网、招聘页、实验网站。主媒体用大图或授权动态，突出设计动作和不可复用部分。
3. 工具型：Figma、Spline 等。展示输入—工作区—结果，而不是把营销首页当工具能力。
4. 元目录型：Toools 一类。展示覆盖地图、组织方式和三个样本目的地，避免假装自己承载全部内容。
5. 知识与比较型：Component Gallery、规范、词条。展示一个概念、关键差异和继续学习入口。

同一结果段内可以保持对齐节奏，但全站不必统一高度。Kimi 的大幅动态案例与 Lucide 的高密度图标样本如果强行同形，只会同时削弱两者。

### 2. 卡片在 1.5 秒内只回答四件事

- 它在当前任务里扮演什么角色。
- 我能从这里拿走什么。
- 为什么系统此刻推荐它。
- 有没有一个会阻止我使用的重要边界。

卡片最小信息结构建议为：

1. 名称 + 当前角色，如“Lucide｜图标系统”。
2. 一句结果导向说明，如“适合先建立一致的线性图标语言”。
3. 一句语境理由，如“与你要求的克制、可扩展方向相符”。
4. 一项关键边界，如“许可已核验”或“只作视觉参考”。
5. 媒体证据。

数量、技术栈、价格和标签只有在当前任务里有判断价值时才出现。不要继续把所有字段压成固定三行。

### 3. 三张图不应只是缩略图拼贴

每张证据必须有编辑标题：

- 先认识它：说明这是哪个产品和它的核心主张。
- 看它的范围：说明内容覆盖和组织方式。
- 看一个真东西：落到具体组件、页面、工具结果或案例。

卡片可以根据类型只显示其中一张、1+2 拼图或授权动态；判断页必须完整显示三份证据及各自来源位置。这样既保留统一证据标准，也避免所有卡片长成同一个相框。

### 4. 判断页不是数据库详情，而是二十秒决策页

建议用从卡片原位放大的悬浮判断页，桌面宽度约占视口的三分之二到四分之三，保留背景上下文；不要做一个塞满字段的小窗。手机端改为全屏页。

信息顺序：

1. 一句话说明它在当前任务中为何出现。
2. “你能拿走什么”和“它不能替你做什么”并列。
3. 三张带标题、来源和深链的证据。
4. “最值得先看的三个位置”，直接进入具体页面，不只给官网首页。
5. 与当前任务相关的正式词条、真实案例或实现资源。
6. 作者、仓库、价格、许可、核验时间等事实区。
7. 固定动作：去源站、加入设计板；符合条件时才出现“分析这个网站”。

标签墙、全局评分和大段自动总结都应取消。用户需要的是取舍理由，不是更多元数据。

## 七、词典、图鉴、场景与外部网站应如何连成一个产品

### 1. 62 与 419 不是同一质量层，不能用总数抹平

给普通人的表达应改成：

- 62 个“可互动词条”：有本地可运行演示、定义和复核。
- 419 个“有来源词条”：有外部定义或案例证据，但尚无本地演示。

419 条可以进入搜索和相关结果，但必须带“尚无本地演示”的明确状态；不要在首页把它们包装成同等完成度的图鉴规模。两条与正式词条重合的记录应折叠为来源补证，不能重复计数。194 个 AI Interaction Atlas 条目如果保留，应另称“交互任务模式”，不能混入视觉现象数量。

### 2. 一个词条应成为连接点，而不是孤立文章

正式词条页至少包含：

1. 一句可记住的定义。
2. 一个可操作演示或清晰证据。
3. “容易和什么混淆”。
4. 在页面哪个位置、什么任务中常见。
5. 使用它的真实网站证据。
6. 能实现它的组件、工具或规范。
7. 加入设计板的动作。

网站判断页反向提供“看它用了什么”视图。用户打开某张证据图时，可选择显示少量标注点，跳到相关词条。不要默认在截图上铺满热区；只有用户进入理解模式时才显示。

### 3. 四种发现方式应并存，但都只是图鉴内部入口

- 知道名称或能描述：自然语言查词。
- 知道自己正在做什么：进入场景解剖，如 Agent 对话区、招聘首页或数据后台。
- 知道大概是什么现象：按布局、动效、组件等宽松现象浏览。
- 想系统补全：按学习路线连续阅读。

场景解剖是空间索引，不是整个产品首页。它的价值是让用户从“底部这个输入区域”找到 Prompt Composer 之类的名称，再继续看到真实案例和实现资源。

截图搜索不能装作确定识别。它应返回两到四个可能词条、置信差异和一个辨别问题，例如“它是在滚动时固定，还是进入视口后才停留？”用户的回答再缩小范围。

### 4. 三者的闭环

最自然的闭环是：

真实网站提供证据 → 图鉴给证据稳定名称 → 场景解剖把位置和名称连接起来 → 用户把案例与词条加入设计板 → 工具把选择翻译成 Agent 说明 → 说明仍能回到原证据。

策展不是一堆外链，词典不是一堆定义，工具也不是一排孤立小应用。它们共同服务“从模糊感觉到有依据的设计决定”。

## 八、Refero：应该借鉴什么，不该照搬什么

### 应借鉴

1. 先建立感觉，再解释机制，最后给机器可读出口的节奏。普通人先看一句记得住的判断，专业用户再看颜色、字体、间距和组件，最后才进入提示词或设计文档。
2. 自然语言搜索与可逛入口并存。搜索不应杀死编辑浏览。
3. Do / Don't 的形式。但每条必须与当前目标和证据相连，不能变成普适审美训诫。
4. 海报先行、按需加载、离开视口暂停、减少动态时不播放的媒体策略。
5. 一个事实模型派生多种输出的思路，而不是为每种格式各写一套互相漂移的文案。

### 不应照搬

1. 不照搬其页面样式、三列节奏、视觉文案和风格隐喻。VisLexicon 面对的是异构资源，不是统一的风格条目。
2. 不把“风格”变成新的主分类。用户的任务可能是实现、辨认、学习或创建，风格只是其中一层。
3. 不照搬没有逐条证据对应的权威口吻。颜色测量、结构事实、系统推断和编辑判断必须分开标示。
4. 不下载、转存或改造 Refero 的视频、截图、数据、完整 DESIGN.md 和导出模板。
5. 不让所有卡片自动播放，也不把动态预览当作“高级感”的装饰。
6. 不把 Trending、Popular、Newest 直接变成首页骨架。它适合浏览排序，不适合替代任务路线。

### 动态预览怎样不破坏三图证据和版权边界

动态片段只承担“先感受”，不承担“证明我们理解了它”。所有正式网站仍必须保存三张身份、范围和实例证据；卡片若使用视频，判断页仍完整展示三张静态证据。

公开片段只接受三类来源：站方明确授权、投稿者提供并确认权利、或许可明确允许再展示的素材。VisLexicon 自己录屏不自动等于取得公开再分发权；许可不清楚时只使用带来源的静态证据，不公开动态片段。Refero 和其他平台已有的片段绝不下载重播。

播放策略应比 Refero 更克制：

- 桌面端同一视口最多播放一个最接近焦点或被悬停/聚焦的片段。
- 手机默认海报，用户点按后播放。
- 静音、行内、5–8 秒、失败退回海报。
- 离开视口暂停；低带宽和减少动态设置下不自动加载。
- 明确标记“授权动态预览”，并保留来源和核验时间。

对于必须靠时间变化理解的词典效果，优先制作 VisLexicon 自有演示，而不是依赖第三方网站录屏。

## 九、URL 转提示词与 DESIGN.md 工作台

这项能力不应作为与策展、词典平级的“工具频道”，更不应成为访问外站的强制中间层。它应从两个具体动作进入：

- 首页输入框粘贴 URL 后选择“分析这个网站”。
- 网站判断页点击“分析这个网站”。

随后进入独立工作台，而不是在浮窗里生成一篇长文。桌面工作台建议为三段：

1. 证据：系统看了哪些公开页面、区域、视口和状态，用户可删除误抓页面。
2. 判断：颜色、排版、间距、组件和交互规则；每条标记“直接观察”“系统推断”“用户确认”。
3. 目标与输出：用户选择网页、移动 App、演示、海报或具体实现环境，再派生简版说明、长版 DESIGN.md、提示词、变量或 JSON。

每条关键规则都必须能点回页面和证据区域。用户修改一条规则时，只更新结构化事实，不整篇重写。JSON 是事实源，Markdown、提示词、Tailwind、CSS 变量只是视图。

工作台必须在分析前处理边界：

- 公开页面可建立来源清单和核验时间。
- 登录态、内部站点和敏感页面默认只做本地临时分析，不进入公共馆藏。
- 分析结果默认属于用户工作区，不自动公开。
- 找不到许可时，不把第三方视觉资产打包进导出。

这项工具的价值不是“把 URL 变成长提示词”，而是让用户检查系统到底看到了什么、同意哪些判断，再把已经确认的设计规则交给 Agent。

## 十、视觉气质：Oreo 只能做一种版面，不应统治全站

温润浅灰画布、白色大圆角底板、紧凑三列和 80px 克制顶栏，适合“最近核验”或某个同质策展集合。它不适合混合结果、词条学习、证据判断和分析工作台。

如果把 32px 白色大底板、圆角卡片、浅紫和浅绿标签、巨型输入框复制到所有页面，产品会很快出现典型的 AI 产品模板味：

- 每块内容都被包进卡片。
- 每个属性都变成彩色胶囊。
- 首页是大句子、大搜索框和一排示例提示。
- 结果仍是规整三列。
- “智能”靠渐变、闪烁和生成动画暗示。

更合适的方向是“视觉杂志 + 证据图谱”：

- 首页用一张主专题、两条次路线和清晰文字目录形成编辑节奏，不做满屏同形卡。
- 图片承担颜色，界面本身保持暖灰、黑白和一个低饱和强调色。
- 用编号、细线、图注、页码感和留白建立可信度；少用胶囊。
- 大图、三联图、密集样本、词条演示和工具工作区各有自己的版式。
- 卡片进入判断页的动画表达空间连续性；其余动效只在内容需要时出现。
- 圆角大底板只用于安静的浏览画布，工作台使用更紧凑、可比较的分栏。

“像一本会理解你的视觉杂志”不是把杂志字体贴到数据库上，而是编辑部替用户决定此刻先看哪一页、为什么看、看完能去哪。

## 十一、桌面文字线框

### 首页第一屏

~~~text
┌──────────────────────────────────────────────────────────────────────┐
│ VISLEXICON                         发现   图鉴   设计板(2)   馆藏说明 │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  把说不清的视觉意图，变成可行动的设计依据。                         │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 描述你正在做什么，或粘贴你看到的网址……       贴 URL  上传截图 │   │
│  └──────────────────────────────────────────────────────────────┘   │
│  例如：带工具调用和审批的 Agent 界面 / 高级但不浮夸的图标语言       │
│                                                                      │
│  本期路线                                                            │
│  ┌────────────────────────────────────┐  01 品牌怎样表达招聘        │
│  │ 大幅主图：做一个可信的 Agent 产品  │  02 从页面位置找到组件名字  │
│  │ 结构 → 案例 → 可用资源 → 设计说明  │  查看全部路线 →             │
│  └────────────────────────────────────┘                              │
├──────────────────────────────────────────────────────────────────────┤
│ 最近核验  [异形媒体条目] [紧凑条目] [紧凑条目]      查看馆藏账本 →   │
└──────────────────────────────────────────────────────────────────────┘
~~~

首页不出现 7 类、59 小类、五个下拉框、8684 候选数量或生产进度轴。

### 任务结果页

~~~text
┌──────────────────────────────────────────────────────────────────────┐
│ VISLEXICON  [带工具调用和审批的 Agent 界面…………………]  设计板(3)    │
├──────────────────────────────────────────────────────────────────────┤
│ 我理解为：先确定消息结构，再找可实现组件，并保留人工审批节点。       │
│ 硬条件：React ×   可商用 ×                  [调整条件] [修改理解]    │
├───────────────────────────────────────────────┬──────────────────────┤
│ 01 先定结构                                   │ 当前设计板           │
│ [工具调用词条/演示] [人类审批场景解剖]        │ 3 个案例，2 个词条   │
│                                               │ 已确认：……           │
│ 02 可以直接试                                 │ 待决定：……           │
│ [21st 等可用型卡] [另一个实现入口]            │                      │
│                                               │ [生成简短任务说明]   │
│ 03 看真实产品怎样取舍                         │                      │
│ [大幅案例] [对照案例]                         │                      │
│                                               │                      │
│ 04 补齐细节                                   │                      │
│ [图标样本] [空状态/动效资源]                  │                      │
└───────────────────────────────────────────────┴──────────────────────┘
~~~

右侧设计板是用户选择的连续状态，不是传统筛选侧栏。结果区段可以因任务改变名称、数量和顺序。

### 卡片与判断页

~~~text
结果卡
┌─────────────────────────────────────────┐
│ [证据媒体或授权动态]                    │
│ 21st.dev｜可直接试                      │
│ 可找到并进入实际组件内容。              │
│ 因含当前需要的交互结构而出现。          │
│ 已核验边界：……                 去源站 ↗│
└─────────────────────────────────────────┘

点击卡片主体
            ↓ 从原位放大
┌────────────────────────────────────────────────────────────────┐
│ 21st.dev           为什么出现在这里                去源站 ↗   │
│ 你能拿走什么                    它不能替你做什么                 │
│ [身份证据]          [范围证据]          [具体实例证据]           │
│ 先看这三个位置：具体页 1 / 具体页 2 / 具体页 3                  │
│ 相关词条与真实案例                                              │
│ 事实：作者 / 仓库 / 价格 / 许可 / 核验时间                      │
│                                  [加入设计板] [分析这个网站]     │
└────────────────────────────────────────────────────────────────┘
~~~

直接点击“去源站 ↗”不打开判断页。判断页里的证据标题可进入具体来源页面，避免所有流量只落到首页。

## 十二、手机文字线框

### 首页

~~~text
┌──────────────────────────────┐
│ VISLEXICON      图鉴  设计板 │
├──────────────────────────────┤
│ 把说不清的视觉意图，          │
│ 变成可行动的设计依据。        │
│ ┌──────────────────────────┐ │
│ │ 说说你正在做什么……      │ │
│ │              URL  截图  ↑│ │
│ └──────────────────────────┘ │
│ 示例：高级但不浮夸的图标语言  │
│                              │
│ 本期路线                      │
│ ┌──────────────────────────┐ │
│ │ 大图：做一个 Agent 产品  │ │
│ │ 结构 → 资源 → 说明       │ │
│ └──────────────────────────┘ │
│ 01 品牌招聘页怎样讲故事       │
│ 02 从页面位置找到专业名称     │
└──────────────────────────────┘
~~~

手机不使用横向滚动的筛选胶囊作为主交互。编辑路线纵向排列，示例提示只保留一条，避免第一屏被教学文案淹没。

### 结果、精确控制与详情

~~~text
┌──────────────────────────────┐
│ [带审批的 Agent 界面……]      │
│ 我理解为：结构 + 可实现组件   │
│ [条件 2]             [板 3]  │
├──────────────────────────────┤
│ 01 先定结构                   │
│ ┌──────────────────────────┐ │
│ │ 媒体 / 演示              │ │
│ │ 工具调用｜正式词条       │ │
│ │ 为什么与你相关          │ │
│ └──────────────────────────┘ │
│ 02 可以直接试                 │
│ ┌──────────────────────────┐ │
│ │ 海报；动态默认不播放     │ │
│ │ 21st.dev                 │ │
│ │ 当前作用 + 关键边界      │ │
│ └──────────────────────────┘ │
└──────────────────────────────┘

点“条件 2” → 底部页，只显示当前任务相关条件
点卡片主体 → 全屏判断页
┌──────────────────────────────┐
│ ← 21st.dev          去源站 ↗ │
│ 为什么在这里                 │
│ 能拿走 / 不能拿走            │
│ [三张证据纵向排列，带图注]   │
│ 最值得先看的位置             │
│ 相关词条                     │
│ 作者 / 许可 / 核验时间       │
│ ─────────────────────────── │
│ [加入设计板]      [去源站 ↗] │
└──────────────────────────────┘
~~~

手机动态预览默认是海报，只有用户点按后播放。全屏判断页底部固定“加入设计板”和“去源站”，不要让用户读到底才找到出口。

## 十三、应立即砍掉和暂缓的内容

### 最应该砍掉的三件事

1. 砍掉前台 7 大类、59 小类和多组下拉筛选。后台可以保留多维字段，但普通用户不应学习编辑部的目录树。
2. 砍掉“8684 条完整资源目录”的公开形态和任何把候选当成果的数量宣传。未完成三图、人话简介、来源与复核的条目不进入正式发现页。
3. 砍掉所有网站共用同一张固定卡、同一三行元数据和一个永久全局分数。改成统一发布标准、按任务计算适配度、按角色选择内容适配器。

### 同时移出首页

- 左侧生产进度竖轴，改放馆藏账本。
- “工具”和“提交”两个顶层频道。
- 满屏彩色标签、技术栈先行和无关体量数字。
- 为所有条目制作视频的目标。
- 把 419 来源记录和未收口的 500+ 数据线包装成完整图鉴。

### 应暂缓的野心

- 全网规模的自然语言万能搜索。
- 自动生成完整长版 DESIGN.md、Tailwind、CSS 和多种令牌的全部出口。
- Agent 自主收录后直接公开。
- 社区市场、评分、热门榜和复杂个性化。
- 给数千候选批量补截图后一次上线。

这些都依赖稳定的证据模型和人工编辑闭环。现在同时推进，只会扩大“看起来完成、内容没做实”的面积。

## 十四、最先做成的闭环

最先做的不是一个新首页，而是一条完整的“意图到设计板”闭环：

> 用户描述任务 → 系统复述意图并确认关键限制 → 给出混合行动路线 → 用户在判断页核验证据 → 访问源站或把案例/词条加入设计板 → 导出一页带来源的 Agent 任务说明。

首期只承诺三条做透的任务路线：

1. 做一个带工具调用和审批的 Agent 产品界面。
2. 做一个有品牌感的招聘官网。
3. 找一套克制、可持续扩展的图标语言。

这三条恰好能压力测试可用组件、单站作品、深度分析、工具、图标、命名库和元目录等混合形态。用户输入超出覆盖范围时，系统应明确显示“目前只有基础检索，尚未形成编辑路线”，不能用生成式文案假装已经理解全世界。

首期设计板只需导出一页短说明：目标、已选案例、已选词条、关键边界、Do / Don't、来源链接和待决定项。不要一开始就承诺完整 DESIGN.md。

## 十五、三阶段路线

### 阶段一：任务型策展产品

独立价值：用户能更快判断哪些资源值得访问，并把研究结果整理成一页可交付说明。

范围：

- 中立的跨内容首页。
- 上述三条完整任务路线，并支持覆盖范围内的自然语言改写。
- 24–36 个真正达到三图证据标准的来源，不展示未核验候选。
- 62 个正式词条中与三条路线相关的部分完成双向连接。
- 多形态结果卡、判断页、直接外跳捷径。
- 轻量设计板与带来源的一页说明。
- 馆藏账本承接核验进度和透明度。

阶段完成的判断不是页面是否好看，而是用户无需打开十个站，就能选出两三个值得继续的来源，并留下可继续使用的决定。

### 阶段二：可识别、可学习的视觉图鉴

独立价值：即使不寻找外部网站，用户也能从名称、描述、截图或页面位置理解视觉现象。

范围：

- 62 个可互动词条形成完整学习与辨析体验。
- 419 个有来源记录逐条复核、去重并明确状态，不按数量一次倾倒。
- 场景解剖、自然语言查词、截图候选识别和系统学习四种入口。
- 词条—证据区域—真实网站—可用实现的双向连接。
- 为必须通过时间理解的词条制作自有动态演示。
- 少量获得明确授权的外站动态预览，验证媒体规则。

完成后，它本身就是一部可信的视觉词典，同时增强阶段一的每一条任务路线。

### 阶段三：证据驱动的设计分析工作台

独立价值：用户能把一个公开网站或自己的页面，转成可检查、可修正、可派生的设计事实和 Agent 上下文。

范围：

- URL 证据采集、页面/区域选择和事实—推断—确认三态。
- 用户修正规则与目标适配。
- JSON 事实源，以及简版/长版 DESIGN.md、提示词和设计变量视图。
- 私密页面本地处理、公共馆藏与个人工作区隔离。
- 从策展判断页进入分析，从分析结果反向链接正式词条。
- 数据稳定后再开放 Agent 接口、投稿去重与协作审核。

阶段三不应以“格式数量”验收，而应以任一关键结论能否回到证据、用户修改后各输出能否一致更新来验收。

## 十六、最终判断

VisLexicon 不缺内容类型，也不缺功能设想；它缺的是一条主线。主线不应是“发现更多设计网站”，而应是“把模糊视觉意图变成有证据的设计决定”。

一旦采用这条主线，很多争论会自动结束：

- 7 大类留在后台，前台展示任务路线。
- 网站不是唯一主角，证据、词条和用户决定同样是内容。
- 人与 Agent 共用一套事实，区别只在阅读与导出。
- 统一的是发布标准，不是卡片形状。
- Refero 值得学的是解释节奏和输出路径，不是素材、页面和权威口吻。
- Oreo 值得保留的是克制与浏览节奏，不是让整个产品变成圆角三列。
- URL 分析是一个由具体对象触发的工作台，不是顶栏里又一个工具仓库。

最关键的一刀，是停止把“候选规模”和“分类完整”当成产品进度。每做实一条从需求、证据、判断到交付的路线，产品才真正前进一步。
