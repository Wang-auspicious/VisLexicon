# VisLexicon 底层策展、分类、去重与真实站点生产交接

**交接日期：** 2026-09-02  
**工作区：** `D:\Desktop\VisLexicon  视元`  
**仓库状态：** 不是 Git 仓库；禁止使用 reset / checkout 等 Git 恢复方式  
**当前范围：** 只做底层分类、标签、去重、链接、证据、截图、简介与发布安全  
**布局状态：** 用户已明确冻结页面布局，由用户后续决定；不得擅自改版

---

## 1. 用户已经明确的要求

1. 必须有分类，但旧“7 类 / 59 小类”逻辑错误，不能继续用。
2. 不能因为用户举了 Agent UI 的例子，就把 Agent / AI 抬成特权类别。AI、Agent、React、移动端、电商、普通招聘等应是横向标签。
3. 分类对象不是整个域名，而是具体 Site Entry；再往下是具体 Content Unit。
4. 每个正式站点必须真实进站探索，选择 identity / breadth / proof 三个互补页面，截三张真图，再写 60–120 字中文人话简介。
5. 不得把目录摘要、机器翻译、“归类为……”套话当成真实理解。
6. 收集、候选、Agent 领取、审核、上传与发布全流程都要去重，但原始 observation 不得被静默删除。
7. 候选数量不等于完成数量。8,684 条候选不能宣称为 8,684 个已理解站点。
8. 不能用类别绑定永久价值分。Kimi 招聘官网对组件安装不相关，对品牌招聘页可能是核心案例。
9. 官方仓库的许可不得扩大到整个网站、第三方图片、付费模板或社区投稿内容。

---

## 2. 已经完成并验证的工作

### 2.1 修复下层目录“点源站没反应”

**根因：** 公共目录 8,684 条记录用 `canonicalUrl`，详情弹层只读 `site.url`，所以域名链接和“访问源站”都没有正确 `href`。

**已修：**

- `demo/src/lib/site-catalog-browser.js`：新增统一 `siteUrl(site)`，优先 `canonicalUrl`，兼容旧 `url`。
- `demo/src/components/SiteDetailModal.jsx`：域名、顶部链接、底部“访问源站”都使用统一 URL。
- 弹层用 React portal 挂到 `document.body`，避免页面滚动后被 `.route` 动画 `transform/filter` 影响而跑出视口。
- `demo/src/SiteCatalog.jsx`：固定索引请求从 `force-cache` 改为 `no-store`，不再长期读到旧 3,229 条缓存。
- 旧 SSR 测试从“卡片必须直接外跳 `<a>`”更新为当前真实交互“卡片按钮打开详情”。

**真实浏览器验证：**

- 实际打开完整目录的 10Web 卡片。
- 页面已滚动 `1268px`时，弹层仍在 `1280×720` 视口内。
- 域名链接和“访问源站”均为 `https://10web.io`。
- 弹层父节点为 `BODY`，控制台 0 错误 / 0 警告。

### 2.2 建立新的对象模型和 13 / 57 分类

**现行规格：** `docs/superpowers/specs/2026-09-01-site-entry-taxonomy-v3-design.md`

**核心对象：**

- `SourceEntity`：稳定的组织、人、产品或项目；本身不带唯一主分类。
- `SiteEntry`：用户进入某个稳定 URL 后能完成的一种主要动作 / 取得的一种主要交付物。
- `ContentUnit`：具体组件、模板、图标包、文章或案例；按自己的内容分类，不继承市场 / 社区容器的类别。
- `ProvisionalIdentityGroup`：仅用强身份信号聚合的临时身份组，不冒充最终实体。

**13 个一级类：**

1. 组件与设计系统
2. 动效、3D 与数据可视化实现
3. 模板与设计文件
4. 图标、字体与视觉素材
5. 设计与内容创作工具
6. 建站、交付与开发工具
7. 研究、测试与无障碍工具
8. 规范、术语与参考
9. 教程、课程与行业内容
10. 案例与灵感集合
11. 资源目录与索引
12. 社区、人才与市场平台
13. 单站案例与作品展示

小类不再强制“每类三个”，实际是 57 个，数量分布 `4/4/4/7/5/4/4/4/4/5/5/3/4`。

**受控标签轴：**

- scenarios
- deliverables
- actions
- media
- platforms
- technologies
- workflowStages
- audiences
- access
- licenses
- contentOrganization
- languages

`source-available` 已作为独立 access 状态，用于 Commons Clause、禁止竞争 / 转售等源码可见但非开源的许可。`AGPL-3.0-only` 和 `AGPL-3.0-or-later` 已进入受控 SPDX 字典。

**代码：**

- `demo/src/data/curation-taxonomy.js`：现行 13 / 57 + facets + 安全验证。
- `demo/src/data/curation-taxonomy-v2-legacy.js`：旧 7 / 59 + score band，只为未迁移 v2 证据临时保留。
- `CONTEXT.md`：已加入 Source Entity、Site Entry、Content Unit、Evidence Attempt、Primary Category、Facet、Task Relevance 等统一术语。
- `AGENTS.md`：已固化布局冻结、entry-level 分类、observation-first 和 PowerShell 规则。

### 2.3 建立 v3 证据合同

`demo/src/lib/curation-evidence.js` 现在安全支持 schema v2 / v3 分流。

v3 bundle 主结构：

```text
entryId + entityId
official
editorial
classification
facets
identity / breadth / proof pages
facts
qa
```

v3 发布门禁：

- 必须 `APPROVED`。
- classification 必须 `confirmed`。
- entity / entry / unit 关系合法。
- curator 与 reviewer 独立。
- editorial reviewer 存在。
- 恰好三页，URL / 截图 / 哈希 / 理由互不重复。
- 截图尺寸、字节、哈希和路径合法。
- 简介 60–120 Unicode 字符，不含禁止套话。
- 受控 facets 通过。
- open-source / source-available / closed-source 互斥，并且必须与受信任 license fact 一致。
- 单站案例必须有直接设计 / 视觉 / 作品理由。
- v3 public projection 不包含 `resourceEssence / score / free-form tags`。
- public shots 保留 `title / selectionRationale / inputUrl / sourceUrl`，可供后续布局使用。

### 2.4 建立保守去重生产队列

**输出：** `demo/data/curation/work-queue-v2.json`

**当前统计：**

- 输入候选：8,684
- 可解析候选行：8,661
- provisional identity groups：8,659
- 完全相同 URL 自动折叠：5 簇 / 5 行
- same-origin / repo-scope 拆分复核簇：242
- quarantine：23，其中旧已知 malformed 17
- 关联严格 approved-v2 证据：6
- needs review：8,653
- 现存可追溯 observations：9,029

**字节稳定性：**

- 大小：9,640,453 bytes
- SHA-256：`a2cfe34a6450572743f547e96683b8b5c915a1b1c681ea49326d8aa1344dcb54`

该文件明确使用 `identityGroups / provisionalIdentityGroups`，没有再虚称“唯一实体”。同域不同路径不自动合并；Figma / Adobe / Mobbin / monorepo 类记录进拆分复核。

### 2.5 collector 已改为 observation-first

**新模块：** `demo/src/lib/source-observation-ledger.js`

**已改 collector：**

- `demo/scripts/collect-npm-resources.mjs`
- `demo/scripts/collect-saaslandingpage.mjs`
- `demo/scripts/collect-toools.mjs`
- `demo/scripts/collect-design-resources.mjs`

新规则：

- 每个返回 raw hit 先变成不可变 Source Observation。
- accepted / rejected 都保留，拒绝有明确原因。
- 同 URL 在多个 query 中命中会保留多条 observation，只在后续 records 层聚合。
- request failure 与 raw hit 分开记录。
- 守恒公式：`returned raw hits = observations`；`request attempts = successful requests + failures`。
- observation ID 是完整 SHA-256。
- getter / Proxy / Symbol / `__proto__` / 循环 / 稀疏数组 / 异常原型全部 fail closed。
- Toools 坏卡和 Design Resources 坏行也会形成 rejected observation，不再静默 `continue`。
- collector import 不再自动联网。

**重要边界：**

现有 `data/sources/*.raw.json` 没有重新生成，修的是未来采集行为。历史 14,843 次查询命中已被旧 collector 提前折叠的部分无法伪造恢复，已记为 provenance debt。

### 2.6 修复 v2 原子发布事务的崩溃窗口

`demo/scripts/migrate-curated-sites-v2.mjs` 原有测试全绿，但独立质量审查仍找到严重缺陷，已修：

- `target -> backup` 和 `temp -> target` 的 rename 前先持久化 `BACKUP_INTENT / PUBLISH_INTENT`。
- 恢复根据 target / temp / backup 实际字节幂等处理。
- PENDING snapshot 留下的 partial temp 可清理。
- 固定 `.lock.recovery` 使用 `wx` 独占，防止两个 stale-owner takeover 同时成功。
- lock / recovery claim / temp / backup / journal 统一做词法、realpath 和 inode 碰撞检查。
- 空 / 截断 / 损坏 owner 和 claim 安全 fail closed，保留恢复证据。
- mutable journal adapter 不再静默降级，直接拒绝。
- adapter 抛字符串 / 冻结 Error 不再掩盖根因或泄漏锁。

**已知剩余风险：**

- Node 现有 adapter 不能可靠 fsync 父目录。
- 校验与 mutation 之间仍有外部恶意进程级 TOCTOU 理论风险。
- backup 还没有原始字节摘要。
- Windows `EPERM / EBUSY` 没有退避重试。
- stale / corrupt recovery claim 为安全起见会阻断，需人工保全 / 隔离后处理。
- 默认 Node 进程存活判断仍无法可靠识别操作系统 PID 复用；可注入完整 owner identity 判断，默认情况下会保守阻断而不是误删。

### 2.7 修复截图底座

`demo/scripts/curation/capture-reviewed-pages.mjs` 新增 / 修复：

- 不再强制只等 `Page.loadEventFired`。
- 允许“主文档 loader 匹配 + 最终 URL 非 about:blank + `readyState=complete` + 主内容足够”作为严格备选。
- 截图前检查视口内 `skeleton / aria-busy / data-loading / data-state=loading`，有占位就拒绝。
- 页面 snapshot 不存在时给清楚错误，不读 `undefined.finalURL`。
- `runCaptures(..., { allowPartial: true })` 支持只补缺图；partial retry 不能冒充完整 review report。

Ant Design 第一张技术成功但带骨架的首页图已从 public 移入：

`demo/data/curation/quarantine/captures/2026-09-01-batch-01/ant-design-v2-identity-skeleton.png`

文件未删除，可恢复，但不得使用。

### 2.8 旧六站已做真实 v3 editorial review

**审核文件：** `demo/data/curation/reviews/2026-09-02-six-v3-editorial-review.json`

- overall verdict：PASS
- reviewer：`independent-six-v3-editorial-reviewer`
- 审核文件 SHA-256：`5917921825E8B246ED7F4D3E642A68D971D9F64F96400C4FE766413AC02B5D6E`
- 6 个 raw v2 bundle SHA-256 全部锁定。
- 18 / 18 官方页面重新读取。
- 18 / 18 截图哈希和 1280×900 尺寸核对。
- 六份简介均为 99–117 字。
- entryId 与 entityId 已分离，例如 `magic-ui` / `entity-magic-ui`。

**关键许可结论：**

- Magic UI：核心仓库 MIT，不扩大到 Pro / Showcase / 第三方内容。
- Coss UI：当前 entry 对应 `cosscom/coss/apps/ui`，该子项目 MIT；根仓库是 AGPL / MIT 混合，不得写“整仓 MIT”。
- Hover.dev：自定义 Hover Dev Component License，是 `source-available + custom`，不是 open-source。
- shadcn/ui：核心仓库 MIT，不扩大到第三方 registry / 依赖。
- Uiverse：官网对 UI elements 声明 MIT；不扩大到广告、头像、评论、外链和商标。
- 21st.dev：Marketplace 没有统一 SPDX，平台条款 + 作者各自许可，是 `source-available + custom`；不得拿某个旁支仓库许可覆盖整站。

**六站评审清单的键合同：**

| 站点 | entityId | entryId | 主类 / 小类 | access / license | 显式修正条数 | 特殊边界 |
|---|---|---|---|---|---:|---|
| Magic UI | `entity-magic-ui` | `magic-ui` | 组件 / 页面区块 | freemium / MIT | 5 | Pro、Showcase 图片不继承 MIT |
| Coss UI | `entity-origin-ui` | `origin-ui` | 组件 / 通用组件 | free / MIT（apps/ui） | 5 | 根仓 AGPL/MIT 混合，不写整仓 MIT |
| Hover.dev | `entity-hover-dev` | `hover-dev` | 组件 / 页面区块 | freemium / custom | 4 | 源码可见但受自定义条款限制 |
| shadcn/ui | `entity-shadcn-ui` | `shadcn-ui` | 组件 / 设计系统套件 | free / MIT | 5 | 依赖和第三方 registry 不继承 |
| Uiverse | `entity-uiverse` | `uiverse` | 组件 / 通用组件 | free / MIT | 5 | 站内 UI elements 与广告、头像、评论分开 |
| 21st.dev | `entity-21st-dev` | `21st-dev` | 组件 / 通用组件 | freemium + login / custom | 4 | Marketplace 由平台条款和各作者许可构成 |

迁移后的 classification curator 使用旧 v2 semantic reviewer 作为语义合同锁定者，classification reviewer 使用新的 `independent-six-v3-editorial-reviewer`；不得把新分类误记成旧 reviewer。

### 2.9 新增三站真实探索已通过独立复核

**批次目录：** `demo/data/curation/research/2026-09-01-batch-01/`

**站点：**

- Shadcn Studio Blocks
- Ant Design
- Chakra UI

**产物：**

- 3 份真实探索草稿。
- 9 张 1280×900 截图。
- 9 个不重复 SHA-256。
- 三份简介长度为 120 / 95 / 96。
- 独立复核文件：`independent-review.json`。

**审核结果：** 3 / 3 PASS，但 `publicationEligible=false`，因为还没有将草稿 + review 组合成正式 immutable v3 bundle，也没有事务发布。

**Shadcn 纠偏：**

首次草稿将整个域名的 Figma / MCP / Templates / Generator 标签混入 Blocks entry，独立 reviewer 判为 NEEDS_CORRECTION。已改成 `Shadcn Studio Blocks`，入口为 `/blocks`，删除跨入口 facets，简介 120 字，原 reviewer 复核后改为 PASS。

---

## 3. 正在进行或已写代码但尚未完成的工作

### 3.1 六站 v2 -> v3 迁移管线

**代码：**

- `demo/scripts/migrate-curated-sites-v3.mjs`
- `demo/tests/curation-migration-v3.test.mjs`

**已完成：**

- 必须显式提供 editorial review manifest；不能默认发布。
- 锁定六站完整集合和每个 v2 raw bundle 的 SHA-256。
- 评审文件严格白名单。
- description / pricing / facts 只能由 hash 锁定的独立 review 明确 add / replace。
- 六站固定 v3 分类映射已锁死，合法但错误的其他 taxonomy 类别也会被拒绝。
- entityId 和 entryId 已使用独立命名空间。
- v3 attemptId 包含旧 attemptId + `v3` + source hash 前 12 位。
- 真实审核 manifest + 当前 6 个 raw v2 bundle 迁移到临时目录已通过。
- 输入 / 输出 / lock / journal 的词法、realpath、inode、junction、symlink、hardlink 检查已加入。
- “初检通过后、读第六个 v2 文件时替换 output junction”的时序已在迁移层于发布前二次检查并拒绝。

**当前测试：**

- `node --test tests/curation-migration-v3.test.mjs`：21 / 21 PASS（当前文件已独立复跑）。
- 上一轮与 evidence / taxonomy / identity / migration-v2 联合基线：164 / 164 PASS；最新发布前二次检查加入后尚未重跑全部 164 项。

**尚未完成的最后安全门：**

v2 `publishOutputTransaction` 尚未支持 `protectedPaths`。应将 6 个 raw v2 输入文件和 inputDir 作为事务受保护命名空间：

1. 事务内部 namespace 预检要包含 protected paths。
2. owner lock 获取且全部 staging 完成后，第一次 backup / publish rename 前再做一次 realpath / inode 复检。
3. 如果发现 output / lock / temp / backup / journal 与 protected input 碰撞，必须走 pre-commit rollback，输入字节不得变。

在这一层完成并由新的只读 reviewer 复核前，**不得把六站正式写入 `approved-v3`**。

### 3.2 混合类别真实站点批次 02

**站点：**

- Laws of UX
- A11Y Project
- ecomm.design

**当前精确文件状态：**

| 站点 | identity | breadth | proof | 人工语义结果 |
|---|---:|---:|---:|---|
| Laws of UX | public 有效 | public 有效 | public 有效 | 3 / 3 已通过 |
| A11Y Project | public 有效 | public 有效 | public 缺失 | proof 两次技术成功但都没露出真实 checklist 项，已隔离，待重拍 |
| ecomm.design | public 有效 | public 缺失 | public 有效 | breadth 两次被 feedback 弹窗遮挡，已隔离，待关闭弹窗后重拍 |

**当前已生成的技术合格截图文件：** 11 张（九个目标角色 + 两次重拍）。其中 public 7 张、quarantine 4 张；7 张通过人工语义复核，2 个目标角色仍缺合格图。

**当前 public 截图：** 7 张。  
**当前 quarantine 截图：** 4 张：

- `a11y-project-v2-proof-top-only.png`
- `a11y-project-v2-proof-intro-only.png`
- `ecomm-design-v2-breadth-feedback-modal.png`
- `ecomm-design-v2-breadth-feedback-modal-2.png`

隔离目录：

`demo/data/curation/quarantine/captures/2026-09-02-batch-02/`

**其他状态：**

- Laws 最终使用官方 Netlify IP 的仅站点 DNS 映射绕过系统代理 TLS 异常，仍然是原官网 / 原三页 / 同一 CDP 门禁。
- 三站官方事实、页面和初步分类研究已做。
- 尚未创建 `demo/data/curation/research/2026-09-02-batch-02/`。
- 尚未写三份 draft JSON 或 batch report。
- 尚未跑该批次的简介长度、classification、facets、9 图唯一性和 taxonomy 验证。
- 该批次还没有独立 reviewer。

---

## 4. 明确尚未做完的事

### 4.1 不得宣称全项目全绿

最后一次已知全项目测试状态仍有 **6 个 Visual Atlas 历史失败**，与本轮策展底层无关：

- Ant Design raw count 70 vs 74。
- 缺一条 `Password Input` 精确翻译。
- 发布 artifact 466 vs 预期 856，以及派生发布 / 索引数量不一致。

这条 Atlas 线应使用单独任务收口，不得为了策展“全绿”去改弱测试。

### 4.2 旧 7 / 59 还没有完全删除

它已不是 canonical taxonomy，但还在：

- `demo/src/data/curation-taxonomy-v2-legacy.js`
- 6 个 approved-v2 bundle
- `work-queue.json`
- migrate-v2 的 legacy semantic contract

只有六站 v3 正式迁移、v3 publisher 和回归全部通过后，才能逐步移除 legacy。

### 4.3 六站还没有正式 approved-v3 文件

- 真实 review manifest 已 PASS。
- 迁移到临时目录已 PASS。
- **正式 `approved-v3` 目录尚未写入。**
- **v3 public revision / manifest / resolver / progress 还没有生成。**
- 受保护输入命名空间的事务内部门禁还缺最后一层。

### 4.4 新三站批次 01 还没有正式 v3 bundle

- 草稿和独立 review 已通过。
- 还没有将 draft + review 组成 immutable v3 bundle。
- 还没有发布到 approved-v3 / public revision。

### 4.5 大部分 8,684 候选仍未真实研读

现在底层能阻止假完成，但不代表内容债务已消失。

已知候选质量债务：

- 8,684 URL 记录，仅 4,419 个不同 hostname。
- 5,477 条分类套话简介。
- 3,207 条机器翻译简介。
- 590 条名称仍为 `Visit Website`。
- 8,365 条无图。
- 101 条只有 1 图。
- 33 条只有 2 图。
- 旧 185 条三图记录仍必须保持 `QUARANTINED_LEGACY`，不得继承发布资格。
- 63 个 public 候选含空图，35 个全部图为空。
- 旧 `siteStatus` 不可信；一次抽样中 12 个旧 `error` 站有 11 个当前可访问。

### 4.6 投稿 / 上传去重尚未完整接到 UI

- `site-identity.js` 和 provisional queue 已提供底层 resolver 能力。
- 未完成公开 resolver revision 与投稿页六种诚实状态接入。
- 不得在没有后端 / publisher 时声称“已进审核队列”。

### 4.7 页面统一呈现尚未做，而且当前不应做

用户已明确表示布局自己会决定。因此本轮只修了源站链接、弹层定位和缓存，没有做统一卡片 / 左侧竖轴 / 首页 / 判断页改版。

### 4.8 Refero / web-to-design-md 后续线未实施

已有调研：

- `docs/research/2026-09-01-refero-styles-web-to-design-md.md`

已确定：

- 借鉴 Refero 的渐进解释、视口感知媒体和 DESIGN.md 拆解节奏，不复制其素材 / 视频 / 文案 / 数据。
- `Paidax01/web-to-design-md` 当时没有许可，不得复制代码。
- 动态预览和 URL -> Design MD 分析是后续独立规格，不是本轮底层完成项。

---

## 5. 接手后的严格执行顺序

### 第一步：完成 protected input 事务门

1. 为 `publishOutputTransaction` 增加可选 `protectedPaths`。
2. 开始事务时检查 target / lock / temp / backup / journal 与 protected input 的词法、realpath、inode 碰撞。
3. 获取 owner 并完成 staging 后、第一次 rename 前再检查。
4. 写通用 v2 transaction 测试 + v3 迁移 junction-swap 测试。
5. 跑 migration-v2 / migration-v3 / evidence / taxonomy / identity 联合回归。
6. 用新的只读 reviewer 复核；不复用已被系统 cyber filter 阻断 final 的旧 reviewer 最终结论。

### 第二步：运行六站真实 v3 迁移

1. 先输出到新目录 `demo/data/curation/approved-v3/`，不覆盖 `approved/`。
2. 使用真实 review manifest，不用 synthetic fixture。
3. 验证 6 个 bundle 全部 `evidenceBundleErrors=[]`。
4. 验证 public projection 没有 legacy fields，三图含标题、理由和深链。
5. 两次迁移字节一致。
6. 只在上述完成后才讨论 public revision publisher。

### 第三步：完成批次 01 的正式 v3 bundle

1. 将三份 draft + `independent-review.json` 组成 immutable v3 bundle。
2. 不得直接把 draft `needs-review` 改字为 confirmed；必须使用 review 中的 confirmedClassification / confirmedFacets。
3. 通过 evidence-v3 validator 和事务发布。

### 第四步：完成批次 02

1. 重拍 A11Y Project proof，必须显示实际 checklist item。
2. 关闭 ecomm.design feedback 弹窗后重拍 breadth。
3. 核对 public / quarantine 目录，只保留最终 9 张 public 图。
4. 写三份 60–120 字草稿和 batch report。
5. 跑 classification / facets / 哈希 / 尺寸 / 空白 / 重复验证。
6. 派独立 reviewer 重新进三个官网复核。

### 第五步：建 v3 revision publisher，不改布局

1. 先原子生成 immutable revision index / resolver / progress。
2. 最后更新小 manifest。
3. 前端读 manifest 用 `no-store`，再读不变 revision URL。
4. 不在这一步决定 UI 布局。

### 第六步：继续混合类别真实站点生产

不要再连续只做组件站。优先按混合批次推进：

- 规范 / 参考：Laws of UX、A11Y Project。
- 行业内容：Nielsen Norman Group。
- 外链目录：Tiny Helpers。
- 案例集合：ecomm.design。
- 创作工具：SVG Doodles。
- 数据可视化实现：Ant Design Charts。
- 后续身份 / 许可集群：Headless UI、MUI、Primer、Carbon、Lucide、Font Awesome、Heroicons、Viewst、Pexels。

### 第七步：最终验证

1. 跑所有策展 / taxonomy / identity / evidence / queue / collector / capture / migration 测试。
2. `npm run lint`。
3. `npm run build`。
4. 单独记录 Visual Atlas 6 个历史失败，不能用它们否定策展回归，也不能忽略它们声称整仓全绿。
5. 用本地浏览器复测下层源站链接、弹层视口和当前 public revision 请求。

---

## 6. 关键文件索引

### 规格 / 上下文

- `AGENTS.md`
- `CONTEXT.md`
- `docs/superpowers/specs/2026-09-01-site-entry-taxonomy-v3-design.md`
- `docs/superpowers/specs/2026-09-01-unified-curation-evidence-pipeline-design.md`（已加纠偏通知，其旧 taxonomy / UI 部分失效）
- `docs/superpowers/plans/2026-09-01-unified-curation-foundation.md`（已加计划更新）
- `VisLexicon-设计评审完整简报-2026-09-01.md`
- `VisLexicon-独立设计评审反馈-2026-09-01.md`
- `Claude-最新完整对话整理.md`

### 分类 / 证据 / 身份

- `demo/src/data/curation-taxonomy.js`
- `demo/src/data/curation-taxonomy-v2-legacy.js`
- `demo/src/lib/curation-evidence.js`
- `demo/src/lib/site-identity.js`
- `demo/src/lib/curation-queue.js`
- `demo/src/lib/source-observation-ledger.js`

### 生产 / 事务 / 截图

- `demo/scripts/migrate-curated-sites-v2.mjs`
- `demo/scripts/migrate-curated-sites-v3.mjs`
- `demo/scripts/curation/capture-reviewed-pages.mjs`
- `demo/scripts/build-curation-queue-v2.mjs`
- `demo/data/curation/work-queue-v2.json`

### 审核 / 研究产物

- `demo/data/curation/reviews/2026-09-02-six-v3-editorial-review.json`
- `demo/data/curation/research/2026-09-01-batch-01/shadcn-studio.json`
- `demo/data/curation/research/2026-09-01-batch-01/ant-design.json`
- `demo/data/curation/research/2026-09-01-batch-01/chakra-ui.json`
- `demo/data/curation/research/2026-09-01-batch-01/batch-capture-report.json`
- `demo/data/curation/research/2026-09-01-batch-01/independent-review.json`

### 重要测试

- `demo/tests/curation-taxonomy.test.mjs`
- `demo/tests/site-identity.test.mjs`
- `demo/tests/curation-evidence.test.mjs`
- `demo/tests/curation-evidence-v3.test.mjs`
- `demo/tests/curation-queue.test.mjs`
- `demo/tests/source-observation-ledger.test.mjs`
- `demo/tests/curation-capture.test.mjs`
- `demo/tests/curation-migration.test.mjs`
- `demo/tests/curation-migration-v3.test.mjs`
- `demo/tests/catalog-browser.test.mjs`
- `demo/tests/curation.test.mjs`

---

## 7. 当前可复核的测试证据

以下是已实际跑过的定向结果，不等于整项目全绿：

- 分类 + v3 evidence（加 AGPL 字典后）：41 / 41 PASS。
- v2 + v3 evidence + taxonomy + identity 联合：117 / 117 PASS（在最后 AGPL 字典只增补受控值前已跑；随后 41 / 41 再次确认相关线）。
- source observation ledger + site catalog：22 / 22 PASS。
- capture pipeline：31 / 31 PASS。
- catalog-browser / capture / evidence-v2 / migration-v2 / queue / taxonomy / curation / modal / identity 九个核心测试文件：当时 211 / 211 PASS。
- migration-v3 当前文件：21 / 21 PASS。
- migration-v3 上一联合基线：164 / 164 PASS；最新二次路径复检后还需重跑。
- `node --check scripts/migrate-curated-sites-v3.mjs`：PASS。

本轮后期尚未重跑：

- `node --test tests/*.test.mjs`
- `npm run lint`
- `npm run build`

---

## 8. 不得再犯的错误

1. 不要把用户举的一个例子升级成特权分类。
2. 不要把整个域名的所有功能标签塞进一个 Entry。Shadcn Studio Blocks 的返工就是直接反例。
3. 不要把“社区驱动”自动归社区类；主要动作必须是参与 / 发布 / 交易 / 撮合。
4. 不要把“图标有 React 包”归成普通组件库；主交付物仍是图标。
5. 不要把颜色 / 样机生成器当成颜色 / 样机素材。
6. 不要因为 GitHub 顶部显示一个 license，就覆盖 monorepo 内具体 entry 或网站社区内容。Coss 是必读反例。
7. 不要把技术成功的截图当成语义成功；骨架占位、反馈弹窗、没露出真实 checklist 都应拒绝。
8. 不要只用 synthetic fixture 证明迁移能跑；真实 review manifest + 真实 raw bundle 必须输出到临时目录通过。
9. 不要在 PowerShell 中直接把 `for / foreach` 语句接管道；先收集到任务专用变量。
10. 不要声称“已完成”而不给测试、哈希、文件和浏览器证据。

---

## 9. 交接时的最终状态一句话

VisLexicon 已经从“错误 7 类 + 8,684 个低标准候选卡”转向“入口级 13 / 57 分类 + 受控标签 + observation-first 去重 + 三页真证据 + 独立复核 + 原子发布”的可扩展底座；但六站还没有正式写入 v3，事务 `protectedPaths` 还缺最后一层，批次 02 还差两张语义合格图，绝大多数候选仍需按该标准逐站研读。

---

## 10. 逐文件、逐模块变更账本

以下是本轮真正动过的文件和它们各自的边界。“完成”只表示该文件的局部目标通过了测试，不表示全站内容已经完成。

### 10.1 项目规则与术语

#### `AGENTS.md`

- 固化工作区不是 Git 仓库。
- 固化页面布局由用户决定，当前只能改底层。
- 固化 entry-level 分类、observation-first、强信号去重与三页证据门。
- 固化 PowerShell 先收集 loop 输出再管道的规则。

#### `CONTEXT.md`

- 更新了策展站点的术语：Source Entity、Site Entry、Content Unit、Source Observation、Provisional Identity Group、Dedup Decision、Evidence Attempt、Approved Entry、Quarantined Candidate、Primary Category、Facet、Quality Fact、Task Relevance。
- 删除了“点卡片直接跳转源站”等与当前弹层行为冲突的定义。
- 文档只存领域语义，不存实现细节。

#### `docs/superpowers/specs/2026-09-01-site-entry-taxonomy-v3-design.md`

- 是现行分类、标签、去重、证据与发布门的权威规格。
- 说明了为什么不能把 AI / Agent 升为一级类，为什么要把容器入口与具体单元分开。
- 补了 source-available、AGPL SPDX、招聘市场作为例外的边界。
- 明确历史 14,843 次查询命中的一部分已被旧 collector 提前折叠，只能承认 provenance debt。

#### `docs/superpowers/specs/2026-09-01-unified-curation-evidence-pipeline-design.md`

- 顶部加了纠偏通知：其中旧 7 / 59、类别固定分数、`agent-ai-ui` 优先线和布局相关部分失效。
- 三页证据、去重、截图 QA、独立复核和原子发布原则仍有效。

### 10.2 分类模块

#### `demo/src/data/curation-taxonomy.js`

- 现行 canonical registry：13 个一级类、57 个不对称小类。
- 每个类别只有 `id / label`，没有 `baseScore / minScore / maxScore`。
- 保存 12 条受控 facet 轴，包括 scenario、deliverable、action、media、platform、technology、workflow、audience、access、license、organization、language。
- `classificationErrors()` 验证 entity / entry / unit 层级、父子类别、状态、alternatives、理由、审核人和 canonical ISO 时间。
- `facetsErrors()` 与 `canonicalizeFacets()` 对字符、Unicode、重复、未知轴、未知值、getter、Proxy、Symbol、`__proto__`、稀疏数组做 fail-closed。
- `isPublishableClassification()` 拒绝 `needs-review / excluded / Visit Website`；单站展示还要额外有三页证据和人工设计理由。

#### `demo/src/data/curation-taxonomy-v2-legacy.js`

- 原 7 / 59 + score band 的原样隔离副本。
- 只允许 v2 evidence 和 migrate-v2 临时使用。
- 不得新建站点再引用，不得把它当成现行类别。

### 10.3 身份、证据与队列模块

#### `demo/src/lib/site-identity.js`

- 规范 HTTP(S) URL，清掉 fragment、跟踪参数、`www`、默认端口和 Git 传输噪声。
- 支持 GitHub shorthand、`git+https` 、`git://`、GitHub / GitLab / Bitbucket 路径规范化。
- 拒绝凭证书、localhost、私网 IP、保留 IP、虚假 DNS 与非 HTTP(S) 协议。
- resolver 区分 `published / candidate / known-alias / suspected-duplicate / new-link / unverifiable`，同域只提醒不自动合并。

#### `demo/src/lib/curation-evidence.js`

- 同一模块按 `schemaVersion` 分流 v2 与 v3，没有让旧数据静默通过新门。
- v2 继续读 legacy taxonomy；v3 只读 canonical taxonomy。
- v3 证据 bundle 包含 `entryId / entityId / editorial / classification / facets / pages / facts / qa`。
- v3 public projection 删除 `resourceEssence / score / tags`，保留每张图的 `title / selectionRationale / inputUrl / sourceUrl`。
- 权利门区分 `open-source / source-available / closed-source`；Commons Clause 不得标 open-source；许可 fact 必须有可追溯直接证据。
- 所有输入先用 descriptor snapshot 快照，拒绝恶意 getter / Proxy / Symbol / 异常原型，避免验证时触发不可信访问。

#### `demo/src/lib/curation-queue.js`

- 从候选索引、已批准 bundle 和可追溯 source catalog 建立临时身份组。
- 只用规范化 URL、明确 entityKey、稳定 repo / package identity 做自动折叠。
- 同 hostname 不同 path 不折叠，进 `ENTRY_SPLIT_REVIEW`。
- 明确区分 `MISSING_SCREENSHOTS / SCREENSHOTS_UNVERIFIED / DESCRIPTION_TEMPLATE / MACHINE_TRANSLATION / LINK_ANOMALY / IDENTITY_CONFLICT / EVIDENCE_GATE_FAILED`。
- 名称为 `identityGroups`，统计为 `provisionalIdentityGroups`，避免把临时组写成最终实体。

#### `demo/src/lib/source-observation-ledger.js`

- 用安全 JSON descriptor snapshot 生成不可变 observation。
- observation ID 为完整 SHA-256，不是易碰撞的短 hash。
- 每个 raw hit 包括 rejected hit 都保留；request failure 单独保留。
- 守恒检查：`returnedRawHits === observations.length`、`requestAttempts === successfulRequests + failures.length`。
- `deriveSourceRecords()` 只在 observation 已被安全快照后才派生记录。

### 10.4 前端功能修复

#### `demo/src/lib/site-catalog-browser.js`

- 新增 `siteUrl(site)`，并给搜索、分页、facet 统计提供纯函数。
- 不改可视布局，只修详情 URL 选择与数据适配。

#### `demo/src/components/SiteDetailModal.jsx`

- 四处链接全部读 `siteUrl(site)`。
- 使用 `createPortal(..., document.body)` 脱离 `.route` 的动画包围块。
- 保留原有详情样式和交互，没有做布局重新设计。

#### `demo/src/SiteCatalog.jsx`

- 固定 index 请求改为 `cache: 'no-store'`。
- 没有把 8,684 候选改成已审核数量；现有提示仍写“候选不等于视觉核验上线”。

### 10.5 collector 变更

#### `demo/scripts/collect-npm-resources.mjs`

- 每个 npm result 先作 observation，再按 URL 派生唯一 records。
- 保留 query、from、ordinal、npm 名、原始 homepage / repository、描述、人气与拒绝原因。
- 每个 listing 显示 observation count、accepted record count、failure count 与状态。
- 增加 main-module guard，import 不再触发网络。

#### `demo/scripts/collect-saaslandingpage.mjs`

- 每个 article，包括坏 URL、缺标题、缺描述的 article，都保留 observation。
- 后续 records 才按 original URL 合并。
- 页面失败作为独立 request failure，不当成死站。

#### `demo/scripts/collect-toools.mjs`

- 每个匹配的 resource card 先作 observation。
- 无 href、坏 URL、无名称、无描述的坏卡进 rejected observation，不静默丢。
- 保留 listingUrl、listing page、ordinal、affiliate redirect 结果。

#### `demo/scripts/collect-design-resources.mjs`

- 每个目标 Markdown table row 先作 observation。
- 坏 link、无名称、无描述行有明确 rejected reason。
- 保留 GitHub README 版本、commit、listing 定位与 fallback failure。

### 10.6 截图与证据生产

#### `demo/scripts/curation/capture-reviewed-pages.mjs`

- 保留 1280×900 固定视口、PNG 解码、尺寸、字节、低熵检查。
- 不再把 `Page.loadEventFired` 当成唯一成功条件；改用 loader-bound readyState、主内容和主文档 HTTP 证据。
- 新增视口内 skeleton / aria-busy / loading 占位拒绝。
- 新增 `allowPartial` 缺图重试方式，但 partial 不能写完整 review report。

#### `demo/scripts/migrate-curated-sites-v2.mjs`

- 原先只有事务测试能跑，本轮加入了意图日志、恢复 claim、命名空间、并发与错误保留。
- `BACKUP_INTENT / PUBLISH_INTENT`、partial temp 回收、stale takeover 独占、journal / inode / hardlink 碰撞均有测试。
- 此文件仍是 v2 事务实现，不代表 v3 已发布。

#### `demo/scripts/migrate-curated-sites-v3.mjs`

- 新增 v2 -> v3 纯转换与六站原子迁移。
- 先读全部 v2 bytes、评审 manifest、派生 v3 bundle 并做公共投影，通过后才组装事务 targets。
- 强制六站固定分类：Magic / Hover 为页面区块，Coss / Uiverse / 21st 按真实入口判定，shadcn/ui 为设计系统套件。
- 支持多条 editorial evidence、按 field 匹配 fact verdict、显式 add / replace fact correction、实体 / 入口分离、迁移来源 hash attemptId。
- 已经有迁移层的二次 path recheck，但还没有把 protected input paths 传入 v2 事务内部。

### 10.7 生成和审核文件

- `demo/data/curation/work-queue-v2.json`：9.6 MB 临时身份队列，不是 public index。
- `demo/data/curation/reviews/2026-09-02-six-v3-editorial-review.json`：六站独立评审、事实修正、许可 scope和 6 个 raw hash。
- `demo/data/curation/research/2026-09-01-batch-01/*.json`：三站草稿、截图报告、独立 review。
- `demo/data/curation/quarantine/captures/2026-09-01-batch-01/ant-design-v2-identity-skeleton.png`：被拒绝的骨架图，保留但禁用。
- `demo/data/curation/quarantine/captures/2026-09-02-batch-02/`：批次 02 的 4 张语义不合格图，不得当成三图证据。

---

## 11. 实际数据流（接手者按这条线继续）

```text
采集返回命中
  ↓ 先写 SourceObservation（accepted / rejected 都保留）
来源文件（原始命中、请求失败、列表位置）
  ↓ 规范化 URL + 强信号折叠
Provisional Identity Group（临时身份组）
  ↓ 身份不明 / 同域多入口 → quarantine / split review
  ↓ 已知候选 → Agent 租约
  ↓ 真实进站：identity + breadth + proof
Evidence Attempt（三图、人话简介、事实、许可）
  ↓ 技术 QA → 独立语义 QA → 必要时编辑 QA
APPROVED v3 bundle
  ↓ 事务原子写入 immutable revision
public index / resolver / 布局消费
```

**不能跳过的节点：**

- 不能从 `sourceIds` 直接推出主分类。
- 不能从一张首页图推出站点范围。
- 不能从 GitHub 仓库根 LICENSE 推出 monorepo 子入口许可。
- 不能从技术成功推出语义合格。
- 不能把 `needs-review` 文件手改成 `APPROVED`。

---

## 12. 批次 01 九张图的逐张状态

| 站点 | 角色 | 原始页面 | 文件 | 状态 | SHA-256 前 12 位 |
|---|---|---|---|---|---|
| Shadcn Studio Blocks | identity | `https://shadcnstudio.com/` | `public/shots/shadcn-studio/v2-identity.png` | PASS | `d53bc77e8d9c` |
| Shadcn Studio Blocks | breadth | `https://shadcnstudio.com/blocks` | `public/shots/shadcn-studio/v2-breadth.png` | PASS | `8f47fabbf233` |
| Shadcn Studio Blocks | proof | `https://shadcnstudio.com/blocks/marketing-ui/hero-section` | `public/shots/shadcn-studio/v2-proof.png` | PASS | `65575106bf3c` |
| Ant Design | identity | `https://ant.design/` → `/index-cn` | `public/shots/ant-design/v2-identity.png` | PASS（重拍） | `38e3dba3b749` |
| Ant Design | breadth | `https://ant.design/components/overview-cn/` | `public/shots/ant-design/v2-breadth.png` | PASS | `a94951799b03` |
| Ant Design | proof | `https://ant.design/components/table-cn#table-demo-basic` | `public/shots/ant-design/v2-proof.png` | PASS | `f1e816c4dbc1` |
| Chakra UI | identity | `https://chakra-ui.com/` | `public/shots/chakra-ui/v2-identity.png` | PASS（改 selector 后） | `b12f3ccbf3ad` |
| Chakra UI | breadth | `https://chakra-ui.com/docs/components/concepts/overview` | `public/shots/chakra-ui/v2-breadth.png` | PASS | `48f59db0ee11` |
| Chakra UI | proof | `https://chakra-ui.com/docs/components/button` | `public/shots/chakra-ui/v2-proof.png` | PASS | `40838eabbdf7` |

九张图共同属性：1280×900、哈希全部不同、不是错误页、不是占位、不是空白。三站独立 reviewer 最终全部 PASS，但正式 v3 bundle 尚未组装。

---

## 13. 批次 02 截图目录的冻结状态

### 13.1 可使用 public 文件（7 张）

| 站点 | 角色 | 字节 | SHA-256 前 12 位 | 尺寸 |
|---|---|---:|---|---|
| Laws of UX | identity | 666,199 | `00e1cfb36223` | 1280×900 |
| Laws of UX | breadth | 161,799 | `a991388306b5` | 1280×900 |
| Laws of UX | proof | 105,711 | `e77af14ab8aa` | 1280×900 |
| A11Y Project | identity | 86,179 | `9667918e6261` | 1280×900 |
| A11Y Project | breadth | 120,128 | `edd80ed69f01` | 1280×900 |
| ecomm.design | identity | 357,107 | `258137d3b611` | 1280×900 |
| ecomm.design | proof | 645,164 | `d377dc907f3a` | 1280×900 |

Laws 的实际页面为首页、`/info/`、`/hicks-law/`；仅因代理 DNS/TLS 异常使用官方 Netlify IP 映射，不是替换站点。

### 13.2 已隔离、禁止使用的文件（4 张）

| 文件 | 字节 | SHA-256 前 12 |
|---|---:|---|
| `a11y-project-v2-proof-top-only.png` | 84,786 | `8cc1b8786ca8` |
| `a11y-project-v2-proof-intro-only.png` | 104,250 | `651ef10a5432` |
| `ecomm-design-v2-breadth-feedback-modal.png` | 726,923 | `f88a6117b8e2` |
| `ecomm-design-v2-breadth-feedback-modal-2.png` | 468,261 | `615b2f121828` |

**原因：**

- A11Y proof 只看到 checklist 说明，没有显示实际检查项。
- ecomm breadth 被 feedback 弹窗遮挡。

**公开目标当前确认为空：** `public/shots/a11y-project/v2-proof.png` 和 `public/shots/ecomm-design/v2-breadth.png` 都不存在。接手时先核对这两个路径，再重拍；不要把隔离图重命名拷回 public。

### 13.3 批次 02 尚未创建的东西

- `demo/data/curation/research/2026-09-02-batch-02/`
- 三份 draft JSON
- batch report
- 独立 reviewer 文件
- 任何 approved v3 bundle

---

## 14. 最新测试矩阵（必须区分时点）

### 当前文件已独立重跑的结果

| 命令 | 结果 | 说明 |
|---|---:|---|
| `node --test tests/curation-migration-v3.test.mjs` | 21 / 21 | 含 post-read junction 聚焦测试，尚未包含 v2 事务 protectedPaths |
| `node --test tests/source-observation-ledger.test.mjs tests/site-catalog.test.mjs` | 22 / 22 | 观测账本与目录合同 |
| `node --test tests/curation-capture.test.mjs` | 31 / 31 | SPA readyState fallback、skeleton gate、partial retry |
| `node --test tests/curation-taxonomy.test.mjs tests/curation-evidence-v3.test.mjs` | 41 / 41 | 后续加了 AGPL / source-available 受控值后重跑 |
| `node --check scripts/migrate-curated-sites-v3.mjs` | PASS | 当前脚本可解析 |
| 相关文件 `npx oxlint ...` | PASS | 本轮文件 0 error / 0 warning |

### 已经有过但不应误用为最新全量结果的数字

- v2 + v3 evidence + taxonomy + identity + migration 相关联合基线曾为 164 / 164；这之后又加了迁移层二次路径复检，应重跑。
- 九个核心策展、截图、证据、去重、迁移测试在之前一轮曾为 211 / 211；不是本次全项目结果。
- 全项目 `node --test tests/*.test.mjs` 、`npm run lint` 、`npm run build` 在本次最后文件状态下尚未重跑。

### 已知 Visual Atlas 独立失败

即使策展底层回归全绿，Visual Atlas 仍有 6 个历史失败：Ant Design raw count 70/74、Password Input 翻译缺失、artifact 466/856 及派生数量不一致。这条线必须单独收口。

---

## 15. 剩余问题的优先级

### P0：不做就不能正式写 v3

1. v2 `publishOutputTransaction` 支持 `protectedPaths`，并在 staging 后第一次 rename 前做受保护输入的 realpath / inode 复检。
2. 用通用事务测试和 v3 junction-swap 测试证明 v2 输入字节零改变。
3. 让新的只读 reviewer 复审这一层，再考虑正式 `approved-v3/`。

### P1：会让公开内容不完整

1. 重拍 A11Y proof，选择能看到真实 checklist 条目的页面。
2. 关闭 ecomm feedback 弹窗后重拍 breadth。
3. 为批次 02 写 draft、独立 review、v3 bundle。
4. 将批次 01 的草稿 + PASS review 组成 v3 bundle。

### P2：会让实施不可持续

1. 建 v3 public revision publisher / resolver / manifest，让前端只读已批准内容。
2. 将 submission resolver 接入新 revision，不再只做本地预检。
3. 重新运行 collectors，产出真正的原始 observation ledger；不要把历史缺失倒写回去。
4. 扩展混合类别批次：Laws / A11Y / NNGroup / Tiny Helpers / ecomm / SVG Doodles / AntV Charts，再做身份与许可集群。

### P3：独立收口

1. 另建 Visual Atlas 收口任务，不与策展底层共享“全部绿”标准。
2. Refero 视频预览、URL -> Design MD 分析、与 web-to-design-md 的结合另写规格，不影响本轮入口分类。

---

## 16. 恢复工作时的操作清单

1. 先读本文件、`AGENTS.md`、`CONTEXT.md`和 v3 taxonomy spec。
2. 先核对 batch 02 public / quarantine 实际文件，不要根据旧代理消息假定。
3. 改事务前先写崩溃 / 竞争失败测试；看到通过不要直接写正式数据。
4. 改分类时先确定是 entity、entry 还是 unit，再写动词句。
5. 改许可时先定 scope：仓库、monorepo 子项目、网站社区内容不能混为一个许可。
6. 每站只有三页证据、真实简介、一手事实和独立 reviewer 全部通过后才能进入正式 bundle。
7. 如果测试、联网、截图或审核失败，在交接日志里写清具体失败原因，不用“暂时可用”等模糊词。
8. 本文件写完后再核对一次实际文件、hash 和测试数字，避免交接内容自己漂移。

---

## 17. 冻结时点

- 本文件是截至 2026-09-02 的实际状态，不是愿景文档。
- 子代理在这一时点均已停止，没有未报告的后台写入。
- 没有正式改写 `demo/data/curation/approved/`、`demo/public/data/site-catalog-index.json`、旧 `work-queue.json`或 public v3 index。
- 最重要的可信句：**底层规则和验证能力已大幅加固，但真正的全量站点理解、正式 v3 发布和页面布局仍然没有完成。**

---

## 18. 2026-09-02 继续工作检查点

本节记录交接之后已经实际写入并重新核对的内容；它不替代前面的冻结约束。

### 18.1 独立设计评审对齐

- 已完整阅读 `VisLexicon-独立设计评审反馈-2026-09-01-v2.md`，并与早一版独立反馈和本文件第 8 节自评逐条对照。
- 评审指出的根因被接受：验证过的内容纪律还没有同样严格地约束产品结构、阶段规模和决策顺序。
- 13 / 57 canonical taxonomy 继续作为后台对象级校验、覆盖审计、去重和二级筛选工具；不得把它重新包装成首页导航、全局价值分或规模承诺。
- 已在 taxonomy spec 顶部写入该边界说明；阶段一公开内容仍以小而完整的真实核验集合为准，8,684 候选只留在内部 resolver / production queue。

### 18.2 protected input 事务门

- `publishOutputTransaction` 现接受 `protectedPaths`，在事务预检和全部 staging 完成后、第一次 rename 前各做一次 lexical / realpath / inode namespace 检查。
- v3 迁移显式传入 raw inputDir 与六个 raw 文件；碰撞走现有 pre-commit rollback，输入字节不变。
- 独立只读 reviewer 复核并重跑 `curation-migration.test.mjs` + `curation-migration-v3.test.mjs`：`49 / 49 PASS`。

### 18.3 正式 v3 与批次 01 / 02

- `demo/data/curation/approved-v3/` 已写入 12 个通过 validator 的 entry bundle：六个旧站、Shadcn Studio Blocks、Ant Design、Chakra UI，以及 Laws of UX、A11Y Project、ecomm.design。
- 批次 01 使用 `assemble-reviewed-v3.mjs` 将 draft + 独立 PASS review 组装；截图目录已按 entryId 补齐，不覆盖旧 siteId 目录。
- 批次 02 新增研究草稿、batch report 和独立 review（review 文件 SHA-256：`dcc483d634cdeac9f9757f724e2962e6b5c9891214e6db3b4965841535e05514`）；三站均 PASS，但 review 的 `publicationEligible` 仍保持 false，正式 bundle 只在本地 validator 通过后生成。
- A11Y proof 最终图显示真实 Content checklist item（1280×900，76,728 bytes，SHA-256 `8048e597691ba7054affc197c1eaf7f50d7265bbca77d97d779cffd5a7e087cb`）。
- ecomm breadth 最终图关闭 feedback survey 并显示多张案例卡（1280×900，808,727 bytes，SHA-256 `fdbd0276374313a5748487f6c7623fe3529d279fe4bf44e5937f22b99d0be155`）。旧失败图仍在 quarantine，未重命名回 public。

### 18.4 revision publisher 与投稿预检

- 新增 `demo/scripts/build-curation-public.mjs` 与 `demo/tests/curation-publisher.test.mjs`：只投影 APPROVED v3，生成稳定 12 位 revision、immutable index / resolver / progress，并最后原子更新 manifest；重复生成保持字节一致。
- 当前 manifest 指向 revision `d10edad626fc`，`publishedCount=12`；index 12 条、无 `resourceEssence / score / tags` legacy 字段，progress 57 条仅用于后台/关于页语境，不含候选总数或 AI 特权线。
- resolver 文件保留候选 exact observations（`candidate-observation`）与诚实 `candidate` 状态，供投稿预检使用；没有把候选当成 public index。
- `preflightSiteSubmission` 已接入纯函数和提交页的只读 manifest/resolver 读取；不会返回 `submitted` / `queued`，网络失败仍明确显示本机草稿、尚未发送。

### 18.5 本检查点后的剩余项

- 页面仍沿用用户冻结的布局；前台任务路线 / 多形态卡片 / 进度移出首页等设计评审建议尚未实现，不能宣称已完成产品重构。
- 公开 revision 目前已生成，但现有 `IndexView` / `SiteCatalog` 仍有旧展示路径；接入新 revision 需另做不改布局的数据迁移和浏览器验收。
- collectors 的历史 provenance debt、绝大多数候选的真实研读、公开投稿后端、Visual Atlas 六个历史失败仍未收口。
- 在下一次全量 `node --test tests/*.test.mjs`、`npm run lint`、`npm run build` 和浏览器复测完成前，不得宣称整仓全绿。

---

## 19. 2026-09-04 Visual Atlas · MDN CSS 进货检查点

- 按用户“先加源、后归类”的既定顺序，正式接入 `mdn-css` 一手源；页面布局保持冻结。
- 固定 MDN revision `366bcbeeeb196a0bc34eaa4e6cdbf244c4ee8354`，许可 `CC-BY-SA-2.5`，原始快照 751 / 751 条、ID 全唯一、必填证据零缺失。
- 计数源 24 → 25，来源记录 1,555 → 2,306，去重词条 1,046 → 1,791；published 仍为 6，新增内容全部保持 candidate。
- 新增确定性 MDN 分类：aesthetic 411、interaction 124、layout 162、motion 54；selector / combinator / at-rule 作为 pattern。
- 修复规范化身份：`@`、`:`、`::` 和尾随 `()` 不再被当作无意义标点抹掉，避免 CSS at-rule、伪类、伪元素和普通术语误合并或误发布。
- Google Translate 429 后，使用隔离 Argos `en_zh 1.9` 补齐 1,500 / 1,500 条翻译，失败 0；新增译文明确标记 machine。
- 最新验证：Atlas 31 / 31、stage-index 13 / 13、整仓 441 / 441、变更文件定向 lint 零输出、Vite build 通过。
- 当前舞台仍只有 173 个 claim / 170 个唯一词条，未入台 1,621；这是新增语料后的真实欠账，不得宣称已经完成舞台建档。
- 完整哈希、分类和验证记录：`docs/verification/2026-09-04-visual-atlas-mdn-source-expansion.md`。

---

## 20. 2026-09-04 Visual Atlas · Figma Plugin API 词汇检查点

- 固定 `figma/plugin-typings` revision `9d38b4222b07c7499bc737fe148e85983ec3b95a`，MIT；解析 `plugin-api.d.ts` 中 14 个明确命名空间。
- 旧审计写 82，当前一手源码实际为 84：主轴对齐新增 `SPACE_EVENLY`、`SPACE_AROUND`。已按真实来源更新合同，没有过滤新记录保旧数字。
- 原始快照 84 / 84、ID 全唯一、证据零缺失；相同 literal 在不同命名空间保持独立。
- 计数源 25 → 26，来源记录 2,306 → 2,390，Atlas 条目 1,791 → 1,875；aesthetic +28、layout +31、motion +25；published 仍为 6。
- 168 / 168 个新 SHA 翻译键由隔离 Argos 模型补齐，失败 0，全部保持 machine 标记。
- 验证：Atlas 33 / 33、stage-index 13 / 13、整仓 443 / 443、变更文件 lint 零输出、全量 lint 退出码 0、Vite build 通过、连续两次实际重建哈希一致。
- 舞台未变：173 个 claim / 170 个唯一词条，未入台 1,705；仍是“先加源”阶段。
- 完整记录：`docs/verification/2026-09-04-visual-atlas-figma-vocabulary-expansion.md`。

---

## 21. 2026-09-04 Visual Atlas · Google Fonts 可变字体轴检查点

- 固定 `googlefonts/axisregistry` revision `c3bd6015d8c8f2a4c85f15226c631c72070c0e62`，Apache-2.0；`Lib/axisregistry/data/` 当前 57 份 textproto。
- 一文件一轴：57 / 57 ID 与 tag 唯一，范围合法，证据零缺失；fallback 刻度与 SVG 不额外计数。
- 解析器支持多行 description、纯尾空格和合法 `#` 行尾注释；两次失败都在写快照前被门禁拦下并以测试复现。
- 计数源 26 → 27，来源记录 2,390 → 2,447，Atlas 条目 1,875 → 1,932；57 条全部是 aesthetic candidate，published 仍为 6。
- 114 / 114 个新翻译键由隔离 Argos 完成，失败 0；缓存总量 4,766。
- 验证：Atlas 35 / 35、stage-index 13 / 13、整仓 445 / 445、变更文件 lint 零输出、全量 lint 退出码 0、Vite build 通过、连续两次实际重建哈希一致。
- 当前舞台 173 claim / 170 唯一词条，未入台 1,762；仍处于用户指定的“先加源”阶段。
- 三批合计新增 892 条一手记录，Atlas 1,046 → 1,932（+886），未提升 published 数。
- 宿主策略拒绝清理本轮 Argos 临时环境；精确路径与约 1.1 GB 占用见 `.learnings/ERRORS.md` 的 `ERR-20260904-004`，项目产物不受影响。
- 完整记录：`docs/verification/2026-09-04-visual-atlas-variable-font-axis-expansion.md`。
